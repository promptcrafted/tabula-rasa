---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - signe/agents/signe.md
  - signe/agents/signe-test-agent.md
  - signe/settings-merge.json
autonomous: true
requirements: [INFRA-01, INFRA-02, INFRA-05, INFRA-10]

must_haves:
  truths:
    - "signe.md is a flat orchestrator agent with tools restricted to Agent(signe-*) for subagent spawning"
    - "signe-test-agent.md is a minimal read-only agent with tools restricted to Read, Glob, Grep"
    - "settings-merge.json contains permission patterns Agent(signe-*) and Skill(signe-* *) alongside existing GSD entries"
    - "settings-merge.json adds SubagentStart and SubagentStop hooks with signe-.* matcher"
    - "Both agents use memory: user for cross-project knowledge persistence"
  artifacts:
    - path: "signe/agents/signe.md"
      provides: "Main orchestrator agent definition"
      contains: "name: signe"
      min_lines: 40
    - path: "signe/agents/signe-test-agent.md"
      provides: "Health check validation agent"
      contains: "name: signe-test-agent"
      min_lines: 20
    - path: "signe/settings-merge.json"
      provides: "Complete settings.json with GSD + Signe entries merged"
      contains: "Agent(signe-*)"
  key_links:
    - from: "signe/agents/signe.md"
      to: "Agent tool"
      via: "tools field restricting spawnable agents"
      pattern: "tools:.*Agent"
    - from: "signe/settings-merge.json"
      to: "signe/hooks/signe-lifecycle.js"
      via: "SubagentStart/Stop hook command paths"
      pattern: "signe-lifecycle\\.js"
    - from: "signe/settings-merge.json"
      to: "Agent(signe-*)"
      via: "permissions.allow array"
      pattern: "Agent\\(signe-\\*\\)"
---

<objective>
Create the two agent definitions (signe.md orchestrator and signe-test-agent.md) and the merged settings.json template that adds Signe's permission patterns and hook entries alongside existing GSD configuration.

Purpose: Establishes the runtime agent infrastructure -- the orchestrator that owns all delegation, the test agent for validation, and the permission/hook configuration that makes everything auto-approved.
Output: 2 agent markdown files + 1 settings template JSON file.
</objective>

<execution_context>
@C:/Users/minta/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minta/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md

<interfaces>
<!-- Existing settings.json structure that MUST be preserved during merge -->
From ~/.claude/settings.json:
```json
{
  "hooks": {
    "SessionStart": [
      { "hooks": [{ "type": "command", "command": "node \"C:/Users/minta/.claude/hooks/gsd-check-update.js\"" }] }
    ],
    "PostToolUse": [
      { "hooks": [{ "type": "command", "command": "node \"C:/Users/minta/.claude/hooks/gsd-context-monitor.js\"" }] }
    ]
  },
  "statusLine": { "type": "command", "command": "node \"C:/Users/minta/.claude/hooks/gsd-statusline.js\"" },
  "autoUpdatesChannel": "latest",
  "effortLevel": "high"
}
```

<!-- Existing GSD agents in ~/.claude/agents/ for naming reference (do NOT touch these) -->
gsd-codebase-mapper.md, gsd-debugger.md, gsd-executor.md, gsd-integration-checker.md,
gsd-nyquist-auditor.md, gsd-phase-researcher.md, gsd-plan-checker.md, gsd-planner.md,
gsd-project-researcher.md, gsd-research-synthesizer.md, gsd-roadmapper.md, gsd-verifier.md
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create agent definitions</name>
  <files>signe/agents/signe.md, signe/agents/signe-test-agent.md</files>
  <action>
Create the `signe/agents/` directory and two agent files.

**signe/agents/signe.md** -- Main orchestrator agent:

YAML frontmatter:
```yaml
name: signe
description: Chief of staff agent. Manages research, planning, design, and oversight workflows by delegating to specialized subagents.
tools: Read, Write, Edit, Bash, Grep, Glob, Agent(signe-*)
model: inherit
memory: user
```

Key frontmatter notes:
- `tools` MUST include `Agent(signe-*)` to restrict which subagents signe can spawn (only signe-prefixed ones). This enforces INFRA-10 flat orchestration -- only signe can spawn, and only signe-prefixed agents.
- `model: inherit` -- uses whatever model the user is running (per research recommendation for Phase 1)
- `memory: user` -- cross-project persistent memory at `~/.claude/agent-memory/signe/`
- Do NOT add `skills:` field yet -- mode skills are Phase 2-5 deliverables
- Do NOT add `maxTurns` -- let it default

Markdown body (the system prompt) should contain:
1. Identity: "You are Signe, a chief of staff agent. You manage research, planning, design, and oversight workflows by delegating to specialized subagents."
2. Flat orchestration rule: "You are the ONLY agent that spawns subagents. Your subagents CANNOT spawn other subagents. All delegation flows through you."
3. Current capabilities section listing what is available NOW vs what is coming:
   - Available: `/signe-health` (installation diagnostic)
   - Coming Phase 2: `/signe-research` (deep-dive research)
   - Coming Phase 3: `/signe-plan` (project planning)
   - Coming Phase 4: `/signe-design` (architecture, UI/UX, agent, product design)
   - Coming Phase 5: `/signe-oversee` (code review, quality verification)
4. If a user asks for a mode that is not yet available, explain which phase will deliver it and suggest they use GSD or manual approaches for now.
5. Behavioral guidelines: proactive, surfaces risks, recommends next actions, uses maker-checker loops.
6. Memory usage: "Use your persistent memory to track validated patterns, model-specific findings, and cross-project learnings."

Keep the body concise (~40-80 lines). The detailed rules are in `~/.claude/rules/signe-*.md` which are auto-loaded.

**signe/agents/signe-test-agent.md** -- Health check agent:

YAML frontmatter:
```yaml
name: signe-test-agent
description: Minimal validation agent for Signe health checks. Read-only, verifies installation completeness.
tools: Read, Glob, Grep
memory: user
```

Key frontmatter notes:
- `tools` is restricted to read-only tools only -- this agent MUST NOT modify files
- `memory: user` so it can report memory status
- No `model` field (defaults to `inherit`)

Markdown body should contain:
1. Role: "You are Signe's health check agent. Your sole purpose is to validate that the Signe installation at ~/.claude/ is complete and functional."
2. Instructions for each check (file existence, YAML validation, memory directory)
3. Report format template with pass/fail for each check
4. Explicit constraint: "Do not modify any files. This is a read-only diagnostic."

Keep the body concise (~20-40 lines).
  </action>
  <verify>
    <automated>grep -c "^---" signe/agents/signe.md &amp;&amp; grep "name: signe$" signe/agents/signe.md &amp;&amp; grep "Agent(signe-\*)" signe/agents/signe.md &amp;&amp; echo "signe.md OK"; grep "name: signe-test-agent" signe/agents/signe-test-agent.md &amp;&amp; echo "test-agent OK"; ls -la signe/agents/</automated>
  </verify>
  <done>signe.md exists with name: signe, tools including Agent(signe-*), memory: user, and system prompt covering identity + flat orchestration + available modes. signe-test-agent.md exists with read-only tools restriction and health check instructions.</done>
</task>

<task type="auto">
  <name>Task 2: Create merged settings.json template</name>
  <files>signe/settings-merge.json</files>
  <action>
Create `signe/settings-merge.json` -- a complete settings.json that merges Signe's entries with the existing GSD configuration. This file serves as the reference template for what `~/.claude/settings.json` should look like after installation.

The file MUST preserve ALL existing GSD entries exactly as they are. Add Signe entries alongside them.

Structure:
```json
{
  "permissions": {
    "allow": [
      "Agent(signe-*)",
      "Skill(signe-* *)"
    ]
  },
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node \"C:/Users/minta/.claude/hooks/gsd-check-update.js\""
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node \"C:/Users/minta/.claude/hooks/gsd-context-monitor.js\""
          }
        ]
      }
    ],
    "SubagentStart": [
      {
        "matcher": "signe-.*",
        "hooks": [
          {
            "type": "command",
            "command": "node \"C:/Users/minta/.claude/hooks/signe-lifecycle.js\""
          }
        ]
      }
    ],
    "SubagentStop": [
      {
        "matcher": "signe-.*",
        "hooks": [
          {
            "type": "command",
            "command": "node \"C:/Users/minta/.claude/hooks/signe-lifecycle.js\""
          }
        ]
      }
    ]
  },
  "statusLine": {
    "type": "command",
    "command": "node \"C:/Users/minta/.claude/hooks/gsd-statusline.js\""
  },
  "autoUpdatesChannel": "latest",
  "effortLevel": "high"
}
```

Critical constraints:
- GSD hooks listed FIRST in their respective arrays, Signe hooks AFTER (per user decision)
- `matcher: "signe-.*"` on SubagentStart/SubagentStop ensures hooks only fire for signe-prefixed agents
- Permission patterns use `Agent(signe-*)` for agent auto-approval and `Skill(signe-* *)` for skill auto-approval (the space + wildcard handles skill arguments)
- Do NOT add `permissions.deny` -- Signe has no deny rules
- Do NOT modify `statusLine`, `autoUpdatesChannel`, or `effortLevel` -- those are existing GSD settings
- Use forward slashes in Windows paths within quotes (JSON string): `C:/Users/minta/.claude/hooks/`

Also add a comment-style header (as a top-level `_comment` field or in a README alongside) explaining:
- This file is the MERGED settings.json template
- To install: replace `~/.claude/settings.json` with this file
- GSD entries are preserved; Signe entries are additive

Actually, JSON does not support comments. Instead, create a brief `signe/INSTALL.md` (~10 lines) with:
1. "Copy all files from `signe/` to `~/.claude/`"
2. "Replace `~/.claude/settings.json` with `signe/settings-merge.json` (renamed to `settings.json`)"
3. "Verify: run `/signe-health` from any project directory"
  </action>
  <verify>
    <automated>node -e "const s = require('./signe/settings-merge.json'); console.log('permissions:', JSON.stringify(s.permissions)); console.log('hooks:', Object.keys(s.hooks)); console.log('SubagentStart matcher:', s.hooks.SubagentStart[0].matcher); console.log('Valid JSON: OK')"</automated>
  </verify>
  <done>settings-merge.json is valid JSON containing: permissions.allow with Agent(signe-*) and Skill(signe-* *), all existing GSD hook entries preserved, SubagentStart and SubagentStop hooks with signe-.* matcher pointing to signe-lifecycle.js. INSTALL.md provides brief installation instructions.</done>
</task>

</tasks>

<verification>
- signe.md contains `tools: ... Agent(signe-*)` enforcing flat orchestration (INFRA-10)
- signe.md has `memory: user` for cross-project knowledge (INFRA-07)
- signe-test-agent.md has read-only tools (Read, Glob, Grep)
- settings-merge.json is valid JSON (verified by `node -e require()`)
- settings-merge.json preserves all GSD entries (SessionStart, PostToolUse, statusLine)
- settings-merge.json adds permissions.allow with Agent(signe-*) and Skill(signe-* *)
- settings-merge.json adds SubagentStart/SubagentStop hooks with signe-.* matcher
- All files use signe- prefix naming
</verification>

<success_criteria>
- Both agent files have valid YAML frontmatter with correct name, tools, and memory fields
- signe.md system prompt covers identity, flat orchestration, available modes, and behavioral guidelines
- signe-test-agent.md is explicitly read-only with health check instructions
- settings-merge.json merges GSD + Signe without losing any existing GSD configuration
- settings-merge.json auto-approves Agent(signe-*) and Skill(signe-* *) patterns
- INSTALL.md provides clear file-drop installation steps
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
