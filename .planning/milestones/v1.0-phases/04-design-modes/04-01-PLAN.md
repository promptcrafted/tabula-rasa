---
phase: 04-design-modes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - signe/agents/signe-designer.md
  - signe/skills/signe-design/SKILL.md
autonomous: true
requirements:
  - ARCH-01
  - ARCH-02
  - ARCH-03
  - ARCH-04
  - ARCH-05
  - ARCH-06
  - UIUX-01
  - UIUX-02
  - UIUX-03
  - UIUX-04
  - UIUX-05
  - AGNT-01
  - AGNT-02
  - AGNT-03
  - AGNT-04
  - AGNT-05
  - PROD-01
  - PROD-02
  - PROD-03
  - PROD-04

must_haves:
  truths:
    - "signe-designer agent definition exists with YAML frontmatter and self-contained system prompt"
    - "Agent handles four presets (architecture, uiux, agent, product) via argument parsing with auto-detection"
    - "Each preset has a complete methodology section with deliverable templates"
    - "/signe-design skill routes to signe-designer agent with fork context"
  artifacts:
    - path: "signe/agents/signe-designer.md"
      provides: "Multi-preset design agent with four complete methodologies"
      min_lines: 350
      contains: "name: signe-designer"
    - path: "signe/skills/signe-design/SKILL.md"
      provides: "Skill entry point for /signe-design"
      contains: "agent: signe-designer"
  key_links:
    - from: "signe/skills/signe-design/SKILL.md"
      to: "signe/agents/signe-designer.md"
      via: "agent: signe-designer in YAML frontmatter"
      pattern: "agent: signe-designer"
---

<objective>
Create the signe-designer agent definition and /signe-design skill entry point.

Purpose: This is the core deliverable of Phase 4 -- a single multi-preset design agent that handles architecture, UI/UX, agent design, and product design through one skill entry point. Follows the validated pattern from signe-researcher (Phase 2) adapted for four distinct design methodologies.

Output: Two files -- signe-designer.md agent definition (~400-500 lines) and signe-design/SKILL.md skill entry point.
</objective>

<execution_context>
@C:/Users/minta/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minta/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-design-modes/04-RESEARCH.md

# Reference patterns (read these to match established conventions)
@signe/agents/signe-researcher.md
@signe/skills/signe-research/SKILL.md
@signe/agents/signe-planner.md
@signe/skills/signe-plan/SKILL.md

<interfaces>
<!-- Existing agent YAML frontmatter pattern (from signe-researcher.md): -->
```yaml
---
name: signe-researcher
description: Multi-source research agent...
tools: Read, Write, Bash, Grep, Glob, WebSearch, WebFetch
mcpServers: brave-search, tavily, exa, context7, arxiv
model: inherit
memory: user
maxTurns: 50
permissionMode: bypassPermissions
---
```

<!-- Existing skill YAML frontmatter pattern (from signe-research/SKILL.md): -->
```yaml
---
name: signe-research
description: Deep-dive research...
context: fork
agent: signe-researcher
disable-model-invocation: false
---
```

<!-- Required YAML frontmatter for signe-designer (from 04-RESEARCH.md): -->
```yaml
---
name: signe-designer
description: Multi-preset design agent. Produces structured design deliverables for system architecture, UI/UX, agent design, and product design.
tools: Read, Write, Bash, Grep, Glob, WebSearch, WebFetch
mcpServers: brave-search, context7, exa
model: inherit
memory: user
maxTurns: 40
permissionMode: bypassPermissions
---
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create signe-designer agent definition</name>
  <files>signe/agents/signe-designer.md</files>
  <action>
Create signe-designer.md with YAML frontmatter and a self-contained system prompt (~400-500 lines). Follow the structure recommended in 04-RESEARCH.md:

**YAML frontmatter** (exact values from research):
- name: signe-designer
- description: Multi-preset design agent. Produces structured design deliverables for system architecture, UI/UX, agent design, and product design.
- tools: Read, Write, Bash, Grep, Glob, WebSearch, WebFetch
- mcpServers: brave-search, context7, exa
- model: inherit
- memory: user
- maxTurns: 40
- permissionMode: bypassPermissions

**System prompt structure** (9 sections, follow this order -- critical instructions near top per Pitfall 1):

1. **Identity and communication style** (10-15 lines): You are Signe's design specialist. Direct, structured output. Produces specifications not implementations. User-centered design mindset.

2. **Argument parsing with preset detection** (30-40 lines): Parse $ARGUMENTS. If first token is `preset:<name>`, use that preset. Valid presets: architecture, uiux, agent, product. Auto-detect keywords per the mapping in 04-RESEARCH.md (architecture keywords, uiux keywords, agent keywords, product keywords). Priority order for ambiguity: agent > architecture > uiux > product. Default to architecture if no match.

3. **Shared design principles** (15-20 lines): User-centered (trace to user need), iterative (structure -> detail -> validate), structured output (tables/headings/templates), traceability (rationale for decisions), research integration (glob for signe-research-*.md before designing).

4. **Preset: Architecture methodology** (60-80 lines):
   - Process: Discover project context (Glob+Read) -> Identify components -> Map data flows -> Define API contracts -> Record decisions -> Specify file structure
   - Deliverables: Component Boundary Table (name, responsibility, interface, dependencies), Data Flow Diagrams (Mermaid graph LR/TD for components, sequenceDiagram for interactions), API Contracts (input types, output types, error cases, versioning), ADRs (decision, context, alternatives, rationale, consequences), File/Folder Structure (annotated tree with purpose comments)
   - Include the exact deliverable templates from 04-RESEARCH.md "Architecture Deliverable Template"
   - Scope sensing: concise for simple systems (1-3 components), focused on critical components for complex systems (10+)
   - Addresses: ARCH-02, ARCH-03, ARCH-04, ARCH-05, ARCH-06

5. **Preset: UI/UX methodology** (60-80 lines):
   - Process: Identify user personas -> Map user flows -> Define component hierarchy -> Create wireframe specs -> Specify accessibility
   - Deliverables: User Flow Maps (entry -> decision points -> outcomes with Mermaid flowcharts), Component Hierarchy (atomic -> composite -> page-level with props/variants table), Wireframe Specs (detailed text specifications with layout grid, content blocks, interaction notes, responsive breakpoints -- NOT HTML by default), Accessibility Requirements (WCAG 2.1 AA baseline, keyboard nav, screen reader support, per-component checklist)
   - Addresses: UIUX-02, UIUX-03, UIUX-04, UIUX-05

6. **Preset: Agent methodology** (60-80 lines):
   - Process: Clarify agent purpose -> Define frontmatter -> Design system prompt -> Select tools -> Package skills
   - Deliverables: Complete YAML frontmatter definition (use the full supported field list from 04-RESEARCH.md: name, description, tools, disallowedTools, model, permissionMode, maxTurns, skills, mcpServers, hooks, memory, background, isolation), System Prompt Structure (role definition, context injection, task methodology, output format, guardrails, examples), Tool Selection Rationale (table with tool, included?, rationale), Skill Definitions (SKILL.md with proper frontmatter)
   - MUST include constraint: subagents cannot spawn other subagents
   - MUST reference the complete supported frontmatter fields list to prevent invalid YAML (Pitfall 4)
   - Addresses: AGNT-02, AGNT-03, AGNT-04, AGNT-05

7. **Preset: Product methodology** (50-70 lines):
   - Process: Define target users -> Generate user stories -> Prioritize features -> Map experience
   - Deliverables: User Stories ("As a [persona], I want [action] so that [value]" with acceptance criteria), MoSCoW Prioritization (table with feature, priority, rationale), Experience Maps (end-to-end user journey across functional milestones with entry points, key interactions, decision points, outcomes)
   - Addresses: PROD-02, PROD-03, PROD-04

8. **Output delivery** (30-40 lines):
   - File naming: `signe-design-[preset]-[slugified-topic].md`
   - Write deliverables to disk using Write tool
   - MUST produce a conversational recap after writing (Pitfall 5): preset used, key design decisions with rationale, deliverables produced with descriptions, open questions, full file path
   - Include the recap template from 04-RESEARCH.md

9. **Safety constraints** (10-15 lines): Do NOT modify existing project files (only create new design documents). Do NOT run destructive Bash commands. Do NOT spawn subagents. Do NOT create actual implementations -- produce specifications only. When this preset is active, follow ONLY its methodology section (prevent cross-contamination per Pitfall 1).

**Critical formatting notes:**
- Use clear section delimiters (`## Preset: Architecture`, `## Preset: UI/UX`, etc.) with explicit "When this preset is active, follow ONLY these steps" at the start of each preset section
- Put identity, argument parsing, and safety near the TOP (most important instructions get highest attention per Pitfall 1)
- Include actual template snippets with placeholder values for each deliverable type (prevent Pitfall 2: vague templates)
- Model the system prompt style after signe-researcher.md (tables over prose for conciseness, numbered steps for process)
  </action>
  <verify>
    <automated>bash -c "test -f signe/agents/signe-designer.md && head -15 signe/agents/signe-designer.md | grep -q 'name: signe-designer' && wc -l < signe/agents/signe-designer.md | awk '{if ($1 >= 350) print \"PASS: \" $1 \" lines\"; else print \"FAIL: only \" $1 \" lines\"}'"</automated>
  </verify>
  <done>signe-designer.md exists with valid YAML frontmatter (name, tools, mcpServers, maxTurns:40, model:inherit, memory:user, permissionMode:bypassPermissions) and system prompt containing all 9 sections. File is 350-550 lines. Each of the 4 preset methodology sections has deliverable templates. Argument parsing section includes auto-detect keyword mapping and priority order.</done>
</task>

<task type="auto">
  <name>Task 2: Create signe-design skill definition</name>
  <files>signe/skills/signe-design/SKILL.md</files>
  <action>
Create signe/skills/signe-design/SKILL.md following the exact pattern from signe-research/SKILL.md.

**YAML frontmatter:**
```yaml
---
name: signe-design
description: Structured design with four presets -- architecture, UI/UX, agent, product
context: fork
agent: signe-designer
disable-model-invocation: false
---
```

**Body** (match the researcher skill body pattern):
```markdown
## Design Task

Create a structured design for the following topic using the appropriate preset.

$ARGUMENTS

If the first token starts with `preset:`, use that preset's methodology.
Otherwise, auto-detect the best preset based on the topic.

Available presets: architecture, uiux, agent, product.

Before designing, check the current directory for research output files
(signe-research-*.md) and incorporate their findings into your design.

Produce structured design deliverables and write them to disk.
```

This is the exact body recommended in 04-RESEARCH.md "Skill Body Pattern" section.
  </action>
  <verify>
    <automated>bash -c "test -f signe/skills/signe-design/SKILL.md && head -10 signe/skills/signe-design/SKILL.md | grep -q 'agent: signe-designer' && echo 'PASS' || echo 'FAIL'"</automated>
  </verify>
  <done>signe-design/SKILL.md exists with correct YAML frontmatter (name: signe-design, context: fork, agent: signe-designer, disable-model-invocation: false) and body containing $ARGUMENTS placeholder, preset instructions, and research integration instruction.</done>
</task>

</tasks>

<verification>
- signe/agents/signe-designer.md exists with valid YAML frontmatter matching research recommendations
- signe/skills/signe-design/SKILL.md exists with correct agent routing
- Agent system prompt contains 4 preset methodology sections with deliverable templates
- Agent system prompt includes argument parsing with preset detection and auto-detect keywords
- File naming convention (signe-design-[preset]-[topic].md) documented in agent prompt
- Recap template included in output delivery section
</verification>

<success_criteria>
- signe-designer.md is 350-550 lines with all 9 system prompt sections
- Each preset methodology has: process steps, deliverable templates with placeholder values, scope sensing
- SKILL.md correctly routes to signe-designer with fork context
- No cross-contamination between preset sections (each starts with "follow ONLY these steps")
- All 20 phase requirements (ARCH-01 through PROD-04) are addressed in the agent's methodology sections
</success_criteria>

<output>
After completion, create `.planning/phases/04-design-modes/04-01-SUMMARY.md`
</output>
