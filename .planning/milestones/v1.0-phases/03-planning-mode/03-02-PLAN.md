---
phase: 03-planning-mode
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - signe/CLAUDE.md
  - signe/agents/signe.md
  - signe/rules/signe-delegation.md
autonomous: false
requirements: [PLAN-01, PLAN-02, PLAN-03, PLAN-04, PLAN-05, PLAN-06, PLAN-07]

must_haves:
  truths:
    - "CLAUDE.md shows Planning mode as Available (not Phase 3 placeholder)"
    - "signe.md lists /signe-plan under Now section (not Coming Soon)"
    - "signe-delegation.md routing table shows signe-planner as Available"
    - "Invoking /signe-plan from any project directory spawns signe-planner agent and produces a structured plan file"
  artifacts:
    - path: "signe/CLAUDE.md"
      provides: "Updated status table with Planning Available"
      contains: "Available"
    - path: "signe/agents/signe.md"
      provides: "Orchestrator with /signe-plan in Now section"
      contains: "/signe-plan"
    - path: "signe/rules/signe-delegation.md"
      provides: "Routing table with signe-planner Available"
      contains: "Available"
  key_links:
    - from: "signe/CLAUDE.md"
      to: "signe/skills/signe-plan/SKILL.md"
      via: "Planning row in Available Modes table"
      pattern: "Available"
    - from: "signe/agents/signe.md"
      to: "signe/agents/signe-planner.md"
      via: "/signe-plan listed under Now section"
      pattern: "signe-plan"
    - from: "signe/rules/signe-delegation.md"
      to: "signe/agents/signe-planner.md"
      via: "Routing table entry"
      pattern: "signe-planner.*Available"
---

<objective>
Update existing Signe infrastructure files to mark Planning mode as Available, deploy all files to ~/.claude/, and validate end-to-end with a real planning task.

Purpose: Following the established Phase 2 integration pattern, this plan updates status tables across three existing files, deploys the new planner agent and skill to the global installation, and runs a human-verified end-to-end test to confirm /signe-plan works from any project directory.

Output: Planning mode fully operational in global ~/.claude/ installation, human-verified working end-to-end.
</objective>

<execution_context>
@C:/Users/minta/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minta/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-planning-mode/03-01-SUMMARY.md

<interfaces>
<!-- Current state of files to be modified. Executor needs these to make targeted updates. -->

From signe/CLAUDE.md (line 17 needs update):
```markdown
| Planning | `/signe-plan` | Goal decomposition with dependency mapping | (Phase 3) |
```
Change to:
```markdown
| Planning | `/signe-plan` | Goal decomposition with dependency mapping | Available |
```

From signe/agents/signe.md (move /signe-plan from Coming Soon to Now):
Current Now section has: /signe-health, /signe-research
Current Coming Soon has: /signe-plan (Phase 3)
Move /signe-plan to Now with description: "Project decomposition, roadmaps, requirements definition, and phase structuring with dependency mapping."

From signe/rules/signe-delegation.md (line 26 needs update):
```markdown
| `/signe-plan` | `signe-planner` | Goal decomposition | (Phase 3) |
```
Change to:
```markdown
| `/signe-plan` | `signe-planner` | Goal decomposition | Available |
```
Also update Phase note from "Phase 2" to "Phase 3" and add signe-planner to the list of available agents.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update existing Signe files and deploy to ~/.claude/</name>
  <files>signe/CLAUDE.md, signe/agents/signe.md, signe/rules/signe-delegation.md</files>
  <action>
**Update three existing files to mark Planning mode as Available:**

1. **signe/CLAUDE.md** -- Change the Planning row status from `(Phase 3)` to `Available` in the Available Modes table.

2. **signe/agents/signe.md** -- Move `/signe-plan` from the "Coming Soon" section to the "Now" section. Add description: "Project decomposition, roadmaps, requirements definition, and phase structuring with dependency mapping." Keep the Coming Soon entry for Phase 4+ items.

3. **signe/rules/signe-delegation.md** -- Change the `/signe-plan` row status from `(Phase 3)` to `Available` in the Skill-to-Agent Routing table. Update the Phase note from "Phase 2 note" to "Phase 3 note" and update the text to list signe-test-agent, signe-researcher, AND signe-planner as available. Update the "do not attempt to spawn" text accordingly.

**Deploy to global installation:**

After updating all three files, copy the entire signe directory to ~/.claude/:
```bash
cp -r signe/agents/signe-planner.md ~/.claude/agents/
cp -r signe/skills/signe-plan/ ~/.claude/skills/
cp signe/CLAUDE.md ~/.claude/CLAUDE.md
cp signe/agents/signe.md ~/.claude/agents/signe.md
cp signe/rules/signe-delegation.md ~/.claude/rules/signe-delegation.md
```

Use direct cp per established Phase 2 deployment pattern -- no installer or symlink indirection.
  </action>
  <verify>
    <automated>bash -c "grep -q 'Available' ~/.claude/CLAUDE.md && grep -q 'signe-plan' ~/.claude/agents/signe.md && grep -q 'Available' ~/.claude/rules/signe-delegation.md && test -f ~/.claude/agents/signe-planner.md && test -f ~/.claude/skills/signe-plan/SKILL.md && echo PASS || echo FAIL"</automated>
  </verify>
  <done>All three files updated with Planning as Available. All planner files deployed to ~/.claude/ global installation. signe-planner.md in ~/.claude/agents/, SKILL.md in ~/.claude/skills/signe-plan/.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end validation of /signe-plan</name>
  <files>none (validation only)</files>
  <action>
Human verifies that the complete planning mode works end-to-end.

**What was built:** Complete planning mode -- signe-planner agent with goal decomposition methodology, /signe-plan skill with argument routing, updated CLAUDE.md/signe.md/delegation rules, all deployed to ~/.claude/.

**How to verify:**
1. Open a NEW Claude Code session (fresh context, any project directory)
2. Run: `/signe-plan Build a REST API for user management with authentication, CRUD operations, and role-based access control`
3. Verify the planner agent spawns (you should see agent activity)
4. Verify the output includes:
   - A plan file written to disk (signe-plan-*.md)
   - Phased decomposition with 3-7 phases
   - Explicit dependencies with justification for each link
   - Verifiable acceptance criteria (YES/NO testable statements, not vague goals)
   - In Scope and Out of Scope tables with rationale columns
   - Ordering rationale addressing dependencies, risk, and value for each phase
   - Dependency map table
   - Risk assessment section
5. Check if research integration works: place a dummy `signe-research-rest-apis.md` file in the cwd before invoking, verify the planner mentions incorporating it (optional advanced test)
6. Verify the conversational recap is concise with phase overview, key decisions, and risks

**Resume signal:** Type "approved" if planning output meets all criteria, or describe specific issues.
  </action>
  <verify>Human confirms planning output quality and structure</verify>
  <done>/signe-plan produces structured plan with phased decomposition, dependency mapping, verifiable acceptance criteria, scope boundaries, and ordering rationale from any project directory without permission prompts.</done>
</task>

</tasks>

<verification>
- Three existing files updated with Planning as Available
- All files deployed to ~/.claude/ global installation
- /signe-plan invocation spawns signe-planner and produces structured plan output
- Plan output contains all required sections: scope, phases, dependencies, acceptance criteria, ordering rationale
</verification>

<success_criteria>
- Planning mode is Available in CLAUDE.md, signe.md, and delegation rules
- /signe-plan produces a structured plan file with phased decomposition, dependency mapping, verifiable acceptance criteria, and scope boundaries
- Human confirms end-to-end validation passes
</success_criteria>

<output>
After completion, create `.planning/phases/03-planning-mode/03-02-SUMMARY.md`
</output>
