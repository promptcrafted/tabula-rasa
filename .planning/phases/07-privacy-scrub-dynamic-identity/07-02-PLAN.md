---
phase: 07-privacy-scrub-dynamic-identity
plan: 02
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - ~/.claude/agents/signe.md
  - ~/.claude/agents/signe-designer.md
  - ~/.claude/agents/signe-overseer.md
  - ~/.claude/agents/signe-planner.md
  - ~/.claude/agents/signe-researcher.md
  - ~/.claude/agents/signe-test-agent.md
  - ~/.claude/rules/signe-delegation.md
  - ~/.claude/rules/signe-personality.md
  - ~/.claude/rules/signe-safety.md
  - ~/.claude/skills/signe/SKILL.md
  - ~/.claude/skills/signe-design/SKILL.md
  - ~/.claude/skills/signe-health/SKILL.md
  - ~/.claude/skills/signe-oversee/SKILL.md
  - ~/.claude/skills/signe-plan/SKILL.md
  - ~/.claude/skills/signe-research/SKILL.md
  - ~/.claude/CLAUDE.md
autonomous: true
requirements:
  - PKG-03

must_haves:
  truths:
    - "Agent loads without errors and all 5 modes function after identity replacement"
    - "Agent uses role-only identity ('I'm your chief of staff agent') before /setup is run"
    - "Agent prompts instruct Claude to adopt persona from MEMORY.md if one exists"
    - "A fresh user with no persona set sees no name, no gendered pronouns — only role references"
    - "One-time subtle /setup hint appears in agent system prompt for first interaction"
    - "Third-person 'Signe' references in rules/skills are replaced with 'The agent' or 'the chief of staff'"
  artifacts:
    - path: "~/.claude/agents/signe.md"
      provides: "Dynamic identity orchestrator with persona-aware prompt"
      contains: "MEMORY.md"
    - path: "~/.claude/rules/signe-personality.md"
      provides: "Personality rules using role references not name"
      contains: "The agent"
    - path: "~/.claude/rules/signe-safety.md"
      provides: "Safety rules using role references"
      contains: "The agent"
    - path: "~/.claude/rules/signe-delegation.md"
      provides: "Delegation rules using role references"
      contains: "The agent"
  key_links:
    - from: "~/.claude/agents/signe.md"
      to: "~/.claude/agent-memory/signe/MEMORY.md"
      via: "Persona loading instruction in system prompt"
      pattern: "MEMORY.md.*persona"
    - from: "~/.claude/agents/signe.md"
      to: "~/.claude/agents/signe-*.md"
      via: "Subagent spawning with persona context"
      pattern: "persona"
    - from: "~/.claude/CLAUDE.md"
      to: "~/.claude/agents/signe.md"
      via: "Template references the agent system"
      pattern: "chief of staff"
---

<objective>
Replace all hardcoded "Signe" identity references with a dynamic persona system that reads from MEMORY.md, falls back to role-only identity, and hints at /setup.

Purpose: PKG-03 requires the agent to work with any persona (or none), not just "Signe". The chief-of-staff personality ships as default behavior; only the name/gender/style become customizable via /setup (Phase 8).
Output: All agent files use dynamic persona pattern. Fallback identity works cleanly. /setup hint included.
</objective>

<execution_context>
@C:/Users/minta/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minta/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-privacy-scrub-dynamic-identity/07-CONTEXT.md
@.planning/phases/07-privacy-scrub-dynamic-identity/07-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace identity in all agent system prompts</name>
  <files>
    ~/.claude/agents/signe.md
    ~/.claude/agents/signe-designer.md
    ~/.claude/agents/signe-overseer.md
    ~/.claude/agents/signe-planner.md
    ~/.claude/agents/signe-researcher.md
    ~/.claude/agents/signe-test-agent.md
  </files>
  <action>
    Read all 6 agent files. Replace the hardcoded "Signe" identity with a dynamic persona pattern.

    **For the main orchestrator (signe.md):**
    Replace the identity line (e.g., "You are Signe, a chief of staff agent") with:
    ```
    You are a chief of staff agent. If your MEMORY.md contains a persona definition (name, personality, style), adopt that identity fully — use that name, match that personality. If no persona is defined, use no name — simply refer to yourself by role ("I'm your chief of staff") and use gender-neutral language (I, me, the agent).
    ```

    Add a one-time hint near the top of the system prompt (after the identity block):
    ```
    On your first interaction with a new user who has no persona defined, include a brief tip: "You can run /setup to personalize me — I'll learn about your work and create my own identity." Do not repeat this hint after the first interaction.
    ```

    **For all subagent files (signe-designer.md, signe-overseer.md, signe-planner.md, signe-researcher.md, signe-test-agent.md):**
    Replace "You are Signe's [role]" or "You are the [role] agent for Signe" patterns with:
    ```
    You are the [role] subagent of a chief of staff agent. If persona context is provided in your task prompt, adopt the same persona. Otherwise, operate without a name using role-only references.
    ```

    Replace ALL remaining "Signe" references in body text of agent files:
    - "Signe can..." → "The agent can..."
    - "Signe's memory" → "The agent's memory"
    - "Ask Signe" → "Ask the orchestrator"
    - "Signe orchestrates" → "The orchestrator manages"
    - Headings like "# Signe" → keep the filename-derived heading but change descriptive text

    Preserve: all behavioral logic, tool allowlists, YAML frontmatter structure, `signe-` prefixes in file references.
  </action>
  <verify>
    <automated>grep -rn "You are Signe\|Signe's\|Signe can\|Signe must\|Signe orchestrat\|Ask Signe\|Signe is\|Signe may" ~/.claude/agents/signe*.md 2>/dev/null; echo "IDENTITY_REFS:$?"</automated>
  </verify>
  <done>Zero hardcoded "Signe" identity references in agent files. Dynamic persona instruction present in signe.md. Subagents reference persona from task context. /setup hint included in main orchestrator.</done>
</task>

<task type="auto">
  <name>Task 2: Replace identity in rules, skills, and CLAUDE.md</name>
  <files>
    ~/.claude/rules/signe-delegation.md
    ~/.claude/rules/signe-personality.md
    ~/.claude/rules/signe-safety.md
    ~/.claude/skills/signe/SKILL.md
    ~/.claude/skills/signe-design/SKILL.md
    ~/.claude/skills/signe-health/SKILL.md
    ~/.claude/skills/signe-oversee/SKILL.md
    ~/.claude/skills/signe-plan/SKILL.md
    ~/.claude/skills/signe-research/SKILL.md
    ~/.claude/CLAUDE.md
  </files>
  <action>
    Read all files. Replace "Signe" with role-based references throughout.

    **Rules files (signe-personality.md, signe-delegation.md, signe-safety.md):**
    - "Signe is a chief of staff" → "The agent is a chief of staff"
    - "Signe must NEVER" → "The agent must NEVER"
    - "Signe must ALWAYS" → "The agent must ALWAYS"
    - "Signe may READ" → "The agent may READ"
    - "Signe recommends" → "The agent recommends"
    - "She manages..." → "The agent manages..." (remove gendered pronouns)
    - "She is not a generic assistant" → "This is not a generic assistant"
    - "She has opinions" → "The agent has opinions"
    - Section headers: "# Signe Personality" → "# Agent Personality" (or similar role-based header)
    - Keep `signe-` in file references (e.g., "See `signe-delegation.md`") — those are filenames, not identity

    **Skill SKILL.md files:**
    - "Signe's [mode]" → "The agent's [mode]" or just "[Mode name]"
    - Description references to "Signe" → "the chief of staff agent" or "the agent"
    - Keep `signe-` in skill/file references — those are filenames

    **CLAUDE.md (template from Plan 01):**
    - Update the agent system intro section to use "chief of staff agent" not "Signe"
    - In the modes table, ensure descriptions don't reference "Signe"
    - The memory section should reference `~/.claude/agent-memory/signe/MEMORY.md` by path (filename stays) but describe it as "agent memory" not "Signe's memory"

    **Preserve:** `signe-` file prefixes everywhere (explicitly out of scope per REQUIREMENTS.md), behavioral content, rule logic, skill routing tables.
  </action>
  <verify>
    <automated>count=$(grep -rn "\bSigne\b" ~/.claude/rules/signe*.md ~/.claude/skills/signe*/SKILL.md ~/.claude/CLAUDE.md 2>/dev/null | grep -v "signe-\|signe/" | wc -l); echo "REMAINING_SIGNE_REFS:$count"</automated>
  </verify>
  <done>Zero "Signe" identity references in rules, skills, and CLAUDE.md (excluding signe-* file prefixes and signe/ directory paths). All references use role-based language. Gender-neutral throughout. Chief-of-staff personality fully intact as default behavior.</done>
</task>

</tasks>

<verification>
Comprehensive check across ALL shipped files:
```bash
# Count remaining "Signe" as identity (not filename prefix)
grep -rn "\bSigne\b" ~/.claude/agents/signe*.md ~/.claude/rules/signe*.md ~/.claude/skills/signe*/SKILL.md ~/.claude/CLAUDE.md 2>/dev/null | grep -v "signe-\|/signe/"

# Verify persona instruction exists in main orchestrator
grep -c "MEMORY.md.*persona\|persona.*MEMORY.md" ~/.claude/agents/signe.md

# Verify /setup hint exists
grep -c "/setup" ~/.claude/agents/signe.md

# Verify gender-neutral fallback
grep -c "gender-neutral\|no name\|by role\|chief of staff" ~/.claude/agents/signe.md
```

Expected: Zero identity refs, persona instruction present, /setup hint present, fallback language present.
</verification>

<success_criteria>
- Zero "Signe" identity references across all shipped files (signe-* prefixes in filenames excluded)
- Main orchestrator has dynamic persona loading instruction referencing MEMORY.md
- Subagents accept persona from task context, fall back to role-only
- One-time /setup hint present in main orchestrator
- Gender-neutral language in all fallback/default text
- Chief-of-staff personality (proactive, opinionated, direct) ships as default behavior — not gated behind /setup
- All agent files parse correctly with valid YAML frontmatter
</success_criteria>

<output>
After completion, create `.planning/phases/07-privacy-scrub-dynamic-identity/07-02-SUMMARY.md`
</output>
