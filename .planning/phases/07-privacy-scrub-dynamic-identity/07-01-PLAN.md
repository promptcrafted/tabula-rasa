---
phase: 07-privacy-scrub-dynamic-identity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/.claude/agents/signe.md
  - ~/.claude/agents/signe-designer.md
  - ~/.claude/agents/signe-overseer.md
  - ~/.claude/agents/signe-planner.md
  - ~/.claude/agents/signe-researcher.md
  - ~/.claude/agents/signe-test-agent.md
  - ~/.claude/rules/signe-delegation.md
  - ~/.claude/rules/signe-personality.md
  - ~/.claude/rules/signe-safety.md
  - ~/.claude/skills/signe/SKILL.md
  - ~/.claude/skills/signe-design/SKILL.md
  - ~/.claude/skills/signe-health/SKILL.md
  - ~/.claude/skills/signe-oversee/SKILL.md
  - ~/.claude/skills/signe-plan/SKILL.md
  - ~/.claude/skills/signe-research/SKILL.md
  - ~/.claude/CLAUDE.md
autonomous: true
requirements:
  - PKG-02

must_haves:
  truths:
    - "No personal file paths (C:\\Users\\minta, alvdansen) remain in any shipped agent file"
    - "No MCP-specific tool configs (Obsidian vault, vexp pipeline) remain in agent files"
    - "No project-specific names (dimljus, girlypop) remain in agent files"
    - "A template CLAUDE.md exists that introduces the agent system with placeholder sections"
    - "SIGNE-GUIDE.md is deleted (Phase 9 creates a proper user guide)"
  artifacts:
    - path: "~/.claude/CLAUDE.md"
      provides: "Template CLAUDE.md for new users"
      contains: "chief of staff"
    - path: "~/.claude/agents/signe.md"
      provides: "Scrubbed orchestrator agent"
    - path: "~/.claude/rules/signe-safety.md"
      provides: "Scrubbed safety rules"
  key_links:
    - from: "~/.claude/CLAUDE.md"
      to: "~/.claude/rules/"
      via: "References rules files for behavioral guidelines"
      pattern: "rules/"
---

<objective>
Audit and remove all personal/private references from every agent file that ships with the project. Create a clean template CLAUDE.md. Delete SIGNE-GUIDE.md.

Purpose: PKG-02 requires the codebase to be free of personal paths, MCP API keys, Obsidian/vexp references, and project-specific content before public release.
Output: All 15+ agent files scrubbed clean. Template CLAUDE.md ready. SIGNE-GUIDE.md removed.
</objective>

<execution_context>
@C:/Users/minta/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minta/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-privacy-scrub-dynamic-identity/07-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and scrub all agent, rule, and skill files</name>
  <files>
    ~/.claude/agents/signe.md
    ~/.claude/agents/signe-designer.md
    ~/.claude/agents/signe-overseer.md
    ~/.claude/agents/signe-planner.md
    ~/.claude/agents/signe-researcher.md
    ~/.claude/agents/signe-test-agent.md
    ~/.claude/rules/signe-delegation.md
    ~/.claude/rules/signe-personality.md
    ~/.claude/rules/signe-safety.md
    ~/.claude/skills/signe/SKILL.md
    ~/.claude/skills/signe-design/SKILL.md
    ~/.claude/skills/signe-health/SKILL.md
    ~/.claude/skills/signe-oversee/SKILL.md
    ~/.claude/skills/signe-plan/SKILL.md
    ~/.claude/skills/signe-research/SKILL.md
  </files>
  <action>
    Read every file listed above. For each file, remove or replace:

    1. **Personal file paths**: Any `C:\Users\minta\...` path — replace with `~/.claude/...` or generic path as appropriate
    2. **Usernames**: `alvdansen` or `minta` — remove or replace with generic references
    3. **MCP tool configs**: Remove specific MCP tool listings that reference Obsidian vault paths, vexp pipeline configs, or user-specific tool setups. Keep generic references to "available MCP tools in the environment" but remove the specific enumeration of Brave, Tavily, Exa, Context7, Obsidian, Puppeteer, etc. from tool descriptions.
    4. **Project names**: `dimljus`, `girlypop`, `dimljus-kit` — remove any references
    5. **Obsidian-specific**: Vault references, port numbers (27123), Local REST API plugin
    6. **vexp-specific**: Pipeline references, daemon mentions, vexp tool listings

    For agent files (signe*.md in agents/): The identity line ("You are Signe...") stays for now — Plan 02 handles dynamic identity replacement. This task ONLY removes private/personal data, not Signe name references.

    For rules files: Same — remove personal data only. "Signe" name references are Plan 02's scope.

    For skill SKILL.md files: Same — remove personal data only.

    Do NOT touch: file prefixes (signe-* stays), "Signe" name references (Plan 02), agent logic/behavior, tool allowlists (just remove personal tool configs from descriptions).
  </action>
  <verify>
    <automated>grep -rn "C:\\\\Users\\\\minta\|alvdansen\|dimljus\|girlypop\|27123\|vexp" ~/.claude/agents/signe*.md ~/.claude/rules/signe*.md ~/.claude/skills/signe*/SKILL.md 2>/dev/null; echo "EXIT:$?"</automated>
  </verify>
  <done>Zero matches for personal paths, usernames, project names, Obsidian ports, or vexp references across all 15 agent/rule/skill files</done>
</task>

<task type="auto">
  <name>Task 2: Replace CLAUDE.md with template and delete SIGNE-GUIDE.md</name>
  <files>
    ~/.claude/CLAUDE.md
    ~/.claude/SIGNE-GUIDE.md
  </files>
  <action>
    1. **Delete SIGNE-GUIDE.md**: Remove `~/.claude/SIGNE-GUIDE.md` entirely. Phase 9 creates a proper user guide from scratch.

    2. **Replace CLAUDE.md** with a clean template that:
       - Introduces the chief of staff agent system (what it is, what modes are available)
       - References `~/.claude/rules/signe-*.md` for behavioral guidelines
       - References `~/.claude/skills/signe-*/` for available skills
       - Has a `## Your MCP Tools` placeholder section with instructions for users to list their own MCP tools
       - Has a `## Your Projects` placeholder section for users to describe their project folders
       - Does NOT contain any personal paths, MCP configs, Obsidian references, vexp references, or project-specific content
       - Does NOT reference dimljus, girlypop, Minta, alvdansen, or any user-specific data
       - Keeps the same general structure (agent intro, available modes table, tool preferences, memory section) but with all content genericized

    The template should feel like a product README that a new user fills in, not a cleaned-up personal config.
  </action>
  <verify>
    <automated>test ! -f ~/.claude/SIGNE-GUIDE.md && grep -c "C:\\\\Users\\\\minta\|alvdansen\|dimljus\|girlypop\|Obsidian\|vexp\|27123" ~/.claude/CLAUDE.md 2>/dev/null; echo "GUIDE_DELETED:$?"; grep -c "chief of staff\|MCP Tools\|Your Projects" ~/.claude/CLAUDE.md</automated>
  </verify>
  <done>SIGNE-GUIDE.md deleted. CLAUDE.md is a clean template with placeholder sections, zero personal references, and introduces the agent system generically.</done>
</task>

</tasks>

<verification>
Run a comprehensive grep across all shipped files for any remaining personal references:
```bash
grep -rn "C:\\Users\\minta\|alvdansen\|dimljus\|girlypop\|27123\|vexp" ~/.claude/agents/signe*.md ~/.claude/rules/signe*.md ~/.claude/skills/signe*/SKILL.md ~/.claude/CLAUDE.md 2>/dev/null
```
Expected: zero matches.

Verify SIGNE-GUIDE.md is deleted:
```bash
test ! -f ~/.claude/SIGNE-GUIDE.md && echo "DELETED" || echo "STILL EXISTS"
```
</verification>

<success_criteria>
- Zero personal references remain in any agent file (paths, usernames, project names, MCP configs)
- Template CLAUDE.md exists with placeholder sections for user customization
- SIGNE-GUIDE.md is deleted
- All agent files still parse correctly (no broken YAML frontmatter, no truncated content)
</success_criteria>

<output>
After completion, create `.planning/phases/07-privacy-scrub-dynamic-identity/07-01-SUMMARY.md`
</output>
