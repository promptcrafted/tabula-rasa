---
phase: 08-setup-workflow
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - ~/.claude/agents/signe-setup-agent.md
  - ~/.claude/skills/signe-reset-persona/SKILL.md
  - ~/.claude/skills/signe-health/SKILL.md
autonomous: false
requirements: [SETUP-04, SETUP-05]

must_haves:
  truths:
    - "User runs /setup inside a project folder and gets a project-scoped persona override"
    - "Project-scoped persona overrides global persona for that project only"
    - "User runs /reset-persona and persona is wiped from MEMORY.md"
    - "After reset, agent returns to pre-setup state with role-only identity"
    - "After reset, /setup hint reappears on next interaction"
  artifacts:
    - path: "~/.claude/skills/signe-reset-persona/SKILL.md"
      provides: "Skill that wipes persona section from MEMORY.md"
    - path: "~/.claude/agents/signe-setup-agent.md"
      provides: "Updated agent with project-scoped setup logic"
  key_links:
    - from: "~/.claude/skills/signe-reset-persona/SKILL.md"
      to: "~/.claude/agent-memory/signe/MEMORY.md"
      via: "Read existing content, remove Persona section, write back"
      pattern: "MEMORY\\.md"
    - from: "~/.claude/agents/signe-setup-agent.md"
      to: "project MEMORY.md"
      via: "Detects project context and writes to project-scoped memory"
      pattern: "project.*persona"
---

<objective>
Add project-scoped persona override to /setup and create /reset-persona skill for wiping persona.

Purpose: Users need project-specific agent personalities (e.g., more formal for a client project, more casual for a side project) and a way to start over if the persona doesn't feel right.

Output: Updated setup agent with project-scope detection, new reset-persona skill, verified end-to-end flow.
</objective>

<execution_context>
@C:/Users/minta/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minta/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-setup-workflow/08-CONTEXT.md
@.planning/phases/08-setup-workflow/08-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 — persona format written to MEMORY.md -->
```markdown
## Persona

- **Name:** [chosen name]
- **Gender:** [chosen gender/pronouns]
- **Personality:** [3-5 descriptors]
- **Style:** [communication approach]
- **User context:** [1-2 sentence summary]
- **Created:** [date]
```

<!-- Claude Code project-scoped memory mechanism -->
<!-- Claude Code uses ~/.claude/projects/{project-hash}/MEMORY.md for project-scoped memory -->
<!-- The `memory: user` scope reads from ~/.claude/agent-memory/signe/MEMORY.md (global) -->
<!-- Project-scoped persona needs to use the project MEMORY.md that Claude Code auto-loads -->

<!-- From signe.md — persona loading priority -->
```markdown
You are a chief of staff agent. If your MEMORY.md contains a persona definition (name, personality, style), adopt that identity fully — use that name, match that personality. If no persona is defined, use no name — simply refer to yourself by role ("I'm your chief of staff") and use gender-neutral language (I, me, the agent).
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add project-scoped setup and create reset-persona skill</name>
  <files>~/.claude/agents/signe-setup-agent.md, ~/.claude/skills/signe-reset-persona/SKILL.md, ~/.claude/skills/signe-health/SKILL.md</files>
  <action>
**1. Update ~/.claude/agents/signe-setup-agent.md — add project-scoped setup logic**

Read the existing agent file (created in Plan 01) and add a new section for project-scoped behavior. Add this logic AFTER the main setup flow:

**Project-scoped setup detection:**
- At the start of setup, check if the user is running /setup from within a project directory by checking if a `.claude/` or project-specific config exists in the current working directory
- If in a project context: run a SHORTER interview focused on "How should I behave differently in THIS project?" (2-3 questions about the project's needs, formality level, domain specifics)
- The project persona inherits everything from global persona (name stays the same) but can override: personality traits, communication style, and add project-specific context
- Write project persona to the project's local MEMORY.md (use `~/.claude/projects/` path based on current working directory, or create a `.claude/MEMORY.md` in the project root — use whichever Claude Code natively supports)
- If no global persona exists yet, run the full global setup first, THEN ask about project-specific overrides

Project persona format (lighter than global):
```markdown
## Project Persona Override

- **Project:** [project name/description]
- **Personality adjustments:** [what changes from global — e.g., "More formal, less humor"]
- **Domain context:** [project-specific knowledge — e.g., "This is a fintech app, precision matters"]
- **Created:** [date]
```

**2. Create ~/.claude/skills/signe-reset-persona/SKILL.md**

YAML frontmatter:
- name: signe-reset-persona
- description: Wipe agent persona and return to pre-setup state. Run /setup again to create a new identity.
- context: none (runs inline, no agent spawn needed — this is a simple file operation)
- disable-model-invocation: false

Body instructions (executed inline by the main orchestrator since context: none):

1. Read `~/.claude/agent-memory/signe/MEMORY.md`
2. Remove the `## Persona` section (everything from `## Persona` to the next `##` heading or end of section)
3. Write back the remaining content
4. Confirm to user: "Persona wiped. I'm back to being your unnamed chief of staff. Run /setup whenever you're ready to create a new identity."
5. If $ARGUMENTS contains "project" or if in a project directory: also remove any `## Project Persona Override` section from the project-scoped MEMORY.md
6. If $ARGUMENTS contains "all": wipe both global AND all project-scoped personas

Do NOT wipe the rest of MEMORY.md (Topics index, agent-recipes reference, learned patterns, etc.). Only the Persona and Project Persona Override sections.

**3. Update ~/.claude/skills/signe-health/SKILL.md**

Add `~/.claude/skills/signe-reset-persona/SKILL.md` to the required files list. Update file count in the report template.
  </action>
  <verify>
    <automated>bash -c "grep -q 'project' ~/.claude/agents/signe-setup-agent.md && test -f ~/.claude/skills/signe-reset-persona/SKILL.md && grep -q 'name: signe-reset-persona' ~/.claude/skills/signe-reset-persona/SKILL.md && grep -q 'signe-reset-persona' ~/.claude/skills/signe-health/SKILL.md && echo 'PASS' || echo 'FAIL'"</automated>
  </verify>
  <done>
    - Setup agent includes project-scoped detection and shorter project interview
    - Project persona format defined with inheritance from global
    - /reset-persona skill exists with correct frontmatter (context: none for inline execution)
    - Reset removes only Persona sections, preserves all other MEMORY.md content
    - Reset supports "project" and "all" arguments
    - Health check updated with new skill file reference
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify end-to-end setup and reset flow</name>
  <action>
Human verifies the complete /setup onboarding and /reset-persona workflow.

What was built:
- /setup conducts casual interview (5-8 questions about work, communication, habits)
- Agent generates and reveals its own name at the end
- Name veto: reject once (agent retries), reject twice (user picks)
- Persona persists to MEMORY.md and loads every session
- /setup in a project folder creates project-scoped persona override
- /reset-persona wipes persona, returns to pre-setup state

How to verify:
1. Run `/setup` in any directory — verify it starts a casual conversation
2. Answer the questions — verify it covers work domain, communication style, work habits
3. Verify it does NOT ask about tools or MCP servers
4. Wait for name reveal — verify it presents with confidence, not as a question
5. Accept the name — verify persona is written to `~/.claude/agent-memory/signe/MEMORY.md`
6. Check that existing MEMORY.md content (Topics, Index) is preserved
7. Start a new conversation — verify agent uses the chosen name and personality
8. Run `/reset-persona` — verify persona section is removed from MEMORY.md
9. Start a new conversation — verify agent is back to "unnamed chief of staff" with /setup hint
  </action>
  <verify>User confirms "approved" or describes issues</verify>
  <done>User approves the end-to-end setup and reset flow works as expected</done>
</task>

</tasks>

<verification>
1. `cat ~/.claude/skills/signe-reset-persona/SKILL.md` — skill exists with context: none
2. `grep -c "project" ~/.claude/agents/signe-setup-agent.md` — project-scoped logic present
3. `grep -c "Persona" ~/.claude/skills/signe-reset-persona/SKILL.md` — references persona removal
4. Run /setup end-to-end and verify persona appears in MEMORY.md
5. Run /reset-persona and verify persona is removed without data loss
</verification>

<success_criteria>
- /setup in project folder runs shorter project-focused interview
- Project persona inherits global name but can override style/personality
- /reset-persona removes persona section(s) only, preserves all other memory
- Reset supports global-only, project-only, and "all" modes
- Human verification confirms end-to-end flow works naturally
</success_criteria>

<output>
After completion, create `.planning/phases/08-setup-workflow/08-02-SUMMARY.md`
</output>
