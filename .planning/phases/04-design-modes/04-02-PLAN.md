---
phase: 04-design-modes
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - signe/CLAUDE.md
  - signe/agents/signe.md
  - signe/rules/signe-delegation.md
autonomous: false

requirements:
  - ARCH-01
  - UIUX-01
  - AGNT-01
  - PROD-01

must_haves:
  truths:
    - "Design mode is marked Available in CLAUDE.md mode table"
    - "signe.md orchestrator lists /signe-design under Now (not Coming Soon)"
    - "signe-delegation.md routes /signe-design to signe-designer with Available status"
    - "All design files are deployed to ~/.claude/ global installation"
    - "User can invoke /signe-design and receive structured design output"
  artifacts:
    - path: "signe/CLAUDE.md"
      provides: "Mode table with Design marked Available"
      contains: "Available"
    - path: "signe/agents/signe.md"
      provides: "Orchestrator with /signe-design in Now section"
      contains: "/signe-design"
    - path: "signe/rules/signe-delegation.md"
      provides: "Routing table with signe-designer Available"
      contains: "Available"
  key_links:
    - from: "signe/CLAUDE.md"
      to: "signe/skills/signe-design/SKILL.md"
      via: "Mode table references /signe-design skill"
      pattern: "signe-design"
    - from: "signe/rules/signe-delegation.md"
      to: "signe/agents/signe-designer.md"
      via: "Routing table maps skill to agent"
      pattern: "signe-designer.*Available"
    - from: "~/.claude/"
      to: "signe/"
      via: "File deployment (cp)"
      pattern: "signe-designer"
---

<objective>
Update Signe infrastructure files to mark Design mode as Available, deploy all design files to ~/.claude/, and validate end-to-end with all four presets.

Purpose: Without integration and deployment, the agent and skill created in Plan 01 are inert source files. This plan makes them live and verifies they work in the real global installation.

Output: Updated CLAUDE.md, signe.md, signe-delegation.md. All files deployed to ~/.claude/. Human verification of /signe-design across all four presets.
</objective>

<execution_context>
@C:/Users/minta/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minta/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior plan output (need to know what files were created)
@.planning/phases/04-design-modes/04-01-SUMMARY.md

# Files to modify (read before editing)
@signe/CLAUDE.md
@signe/agents/signe.md
@signe/rules/signe-delegation.md

<interfaces>
<!-- Current CLAUDE.md mode table row for Design (to update): -->
```
| Design | `/signe-design` | Four presets: architecture, UI/UX, agent, product | (Phase 4) |
```
Update to:
```
| Design | `/signe-design` | Four presets: architecture, UI/UX, agent, product | Available |
```

<!-- Current signe.md Coming Soon section (to move to Now): -->
```
- `/signe-design` (Phase 4) -- Four design presets under one entry point:
  - Architecture: component boundaries, data flow, API design
  - UI/UX: wireframes, user flows, visual direction
  - Agent: creating new agents with tested prompts and workflows
  - Product: feature scoping, user stories, experience mapping
```

<!-- Current signe-delegation.md routing row (to update): -->
```
| `/signe-design` | `signe-designer` | Architecture, UI/UX, agent, product design | (Phase 4) |
```
Update to:
```
| `/signe-design` | `signe-designer` | Architecture, UI/UX, agent, product design | Available |
```

<!-- Phase note to update: -->
```
**Phase 3 note:** `signe-test-agent`, `signe-researcher`, and `signe-planner` are available.
```
Update to:
```
**Phase 4 note:** `signe-test-agent`, `signe-researcher`, `signe-planner`, and `signe-designer` are available.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Signe infrastructure files and deploy to ~/.claude/</name>
  <files>signe/CLAUDE.md, signe/agents/signe.md, signe/rules/signe-delegation.md</files>
  <action>
Update three existing files following the exact pattern established in Phase 2 (02-02) and Phase 3 (03-02):

1. **signe/CLAUDE.md**: Change Design row status from `(Phase 4)` to `Available` in the mode table.

2. **signe/agents/signe.md**: Move the `/signe-design` entry from the "Coming Soon" section to the "Now" section. Add it as:
   ```
   - `/signe-design` -- Structured design with four presets: architecture (component boundaries, data flow, API contracts, ADRs), UI/UX (user flows, wireframes, component hierarchy, accessibility), agent (YAML frontmatter definitions, system prompts, tool selection), product (user stories, MoSCoW prioritization, experience maps).
   ```
   Remove the `/signe-design (Phase 4)` block from Coming Soon. The Coming Soon section should only have `/signe-oversee (Phase 5)` remaining.

3. **signe/rules/signe-delegation.md**: Change `/signe-design` routing row status from `(Phase 4)` to `Available`. Update the phase note from "Phase 3 note" to "Phase 4 note" and add `signe-designer` to the list of available agents.

4. **Deploy to ~/.claude/**: Copy all signe files to the global installation:
   ```bash
   cp -r signe/agents/* ~/.claude/agents/
   cp -r signe/skills/* ~/.claude/skills/
   cp signe/CLAUDE.md ~/.claude/CLAUDE.md
   cp -r signe/rules/* ~/.claude/rules/
   ```
   This deploys both the new files (signe-designer.md, signe-design/SKILL.md) and the updated infrastructure files.
  </action>
  <verify>
    <automated>bash -c "grep -q 'Available' signe/CLAUDE.md && grep -q 'signe-design' signe/agents/signe.md && grep -q 'Available' signe/rules/signe-delegation.md && test -f ~/.claude/agents/signe-designer.md && test -f ~/.claude/skills/signe-design/SKILL.md && echo 'PASS' || echo 'FAIL'"</automated>
  </verify>
  <done>Design mode marked Available in all three infrastructure files. All signe files (agents, skills, rules, CLAUDE.md) deployed to ~/.claude/. signe-designer.md and signe-design/SKILL.md exist in ~/.claude/ global installation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end validation of /signe-design with all four presets</name>
  <files>N/A</files>
  <action>
Human validates that /signe-design works end-to-end by invoking each preset and inspecting the output. This is a human-verify checkpoint -- the executor pauses and asks the user to test.

Test each preset from any project directory. Run ALL FOUR to validate cross-preset isolation:

1. **Architecture preset:**
   ```
   /signe-design preset:architecture Design a REST API gateway for a microservices e-commerce platform
   ```
   Expected: Output file `signe-design-architecture-*.md` with Component Boundary Table, Mermaid Data Flow Diagrams, API Contracts, ADRs, and annotated File/Folder Structure.

2. **UI/UX preset:**
   ```
   /signe-design preset:uiux Design the onboarding flow for a project management app
   ```
   Expected: Output file `signe-design-uiux-*.md` with User Flow Maps, Component Hierarchy (atomic/composite/page), Wireframe text specs, and WCAG 2.1 AA Accessibility Requirements.

3. **Agent preset:**
   ```
   /signe-design preset:agent Design a code review agent for Claude Code
   ```
   Expected: Output file `signe-design-agent-*.md` with complete YAML frontmatter (using only supported fields), System Prompt Structure (role/context/task/output/guardrails), Tool Selection Rationale table, and Skill definitions.

4. **Product preset:**
   ```
   /signe-design preset:product Design the MVP for a habit tracking app
   ```
   Expected: Output file `signe-design-product-*.md` with User Stories (As a.../I want.../So that...) with acceptance criteria, MoSCoW Prioritization table with rationale, and Experience Map with milestones.

For each preset verify:
- Agent spawns successfully (no permission errors)
- Output file is written to disk with correct naming convention
- Conversational recap appears after file write
- Deliverables match the expected template structure
- No cross-contamination (architecture deliverables in UI/UX output, etc.)
  </action>
  <verify>User confirms all four presets produce correct structured output by typing "approved"</verify>
  <done>All four design presets (architecture, UI/UX, agent, product) produce structured deliverables matching their respective templates. No cross-contamination between presets. Agent spawns without permission errors from any project directory.</done>
</task>

</tasks>

<verification>
- CLAUDE.md shows Design as Available (not Phase 4)
- signe.md lists /signe-design under Now section
- signe-delegation.md has signe-designer as Available with Phase 4 note
- ~/.claude/agents/signe-designer.md exists
- ~/.claude/skills/signe-design/SKILL.md exists
- All four presets produce structured output when invoked via /signe-design
</verification>

<success_criteria>
- All three infrastructure files updated with correct status
- Global deployment to ~/.claude/ includes all new and updated files
- Human verifies at least one preset invocation produces correct structured output
- No permission errors when invoking /signe-design from any project directory
- Agent spawns, writes deliverable file, and produces conversational recap
</success_criteria>

<output>
After completion, create `.planning/phases/04-design-modes/04-02-SUMMARY.md`
</output>
