---
phase: 05-oversight-memory
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - signe/agents/signe.md
  - ~/.claude/agent-memory/signe/MEMORY.md
  - ~/.claude/agent-memory/signe/agent-recipes.md
autonomous: true
requirements:
  - METH-01
  - METH-02
  - METH-03
  - METH-04
  - METH-05

must_haves:
  truths:
    - "signe.md contains a Subagent Methodology section with the 5-step cycle (research, design, test, validate, bank)"
    - "MEMORY.md index references agent-recipes.md as a topic file"
    - "agent-recipes.md exists with entry format template and instructions"
    - "Methodology instructs Signe to research before designing (METH-01)"
    - "Methodology references signe-designer agent preset for structured prompt engineering (METH-02)"
    - "Methodology defines dry-run testing with 2-3 sample tasks (METH-03)"
    - "Methodology defines concrete validation criteria (METH-04)"
    - "Methodology defines banking to agent-recipes.md with required metadata fields (METH-05)"
  artifacts:
    - path: "signe/agents/signe.md"
      provides: "Orchestrator with methodology guidelines"
      contains: "Subagent Methodology"
    - path: "~/.claude/agent-memory/signe/agent-recipes.md"
      provides: "Validated agent pattern playbook"
      contains: "Validated Agent Patterns"
    - path: "~/.claude/agent-memory/signe/MEMORY.md"
      provides: "Memory index with topic reference"
      contains: "agent-recipes.md"
  key_links:
    - from: "signe/agents/signe.md"
      to: "~/.claude/agent-memory/signe/agent-recipes.md"
      via: "Methodology step 5 references agent-recipes.md for banking"
      pattern: "agent-recipes"
---

<objective>
Add subagent methodology guidelines to signe.md and initialize the agent-recipes memory topic file.

Purpose: Gives Signe a systematic process for creating new agents (research -> design -> test -> validate -> bank) instead of ad-hoc prompt writing, and establishes the persistent playbook where validated patterns are stored.
Output: Updated signe.md with methodology section, initialized agent-recipes.md topic file, updated MEMORY.md index.
</objective>

<execution_context>
@C:/Users/minta/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minta/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-oversight-memory/05-RESEARCH.md

Reference files (read before implementing):
@signe/agents/signe.md (current orchestrator -- will be modified)

<interfaces>
<!-- Current signe.md structure that must be preserved -->

From signe/agents/signe.md:
- YAML frontmatter (name, description, tools, model, memory)
- Flat Orchestration section
- Available Capabilities section (Now + Coming Soon)
- Behavioral Guidelines section
- Memory section
- Tool Access section

The methodology section should be added AFTER the Memory section and BEFORE Tool Access.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add subagent methodology to signe.md</name>
  <files>signe/agents/signe.md</files>
  <action>
Read signe/agents/signe.md. Add a new `## Subagent Methodology` section AFTER the existing `## Memory` section and BEFORE `## Tool Access`. Do NOT modify any existing sections.

The methodology section content (from research Pattern 6):

```markdown
## Subagent Methodology

When creating a NEW agent (not using an existing one), follow this cycle:

### 1. Research

Spawn `/signe-research` to investigate:
- Prompt patterns that work for this task type and model
- Tool requirements and permission considerations
- Known pitfalls and failure modes
- Community examples of similar agents

### 2. Design

Spawn `/signe-design preset:agent` with research findings as context:
- Produces YAML frontmatter, system prompt, tool rationale, skill definition
- Uses structured methodology (role, context, methodology, output, guardrails)
- NOT ad-hoc prompt writing

### 3. Test

Create the agent files, then invoke with 2-3 sample tasks:
- One simple task (baseline competence)
- One medium-complexity task (normal usage)
- One edge case or adversarial task (robustness)
- Evaluate: Does output follow expected format? Are findings specific? No hallucinations?

### 4. Validate

Check results against concrete criteria:
- Output follows expected structure and format
- Findings reference real files and lines (not hallucinated)
- Severity assignments are appropriate (not all critical, not all low)
- Recommendations are actionable (not vague advice)
- Success rate: at least 2 out of 3 sample tasks must pass

### 5. Bank or Iterate

If validated, save pattern to `~/.claude/agent-memory/signe/agent-recipes.md`:
- Model used, task type, prompt pattern summary
- What worked well, what needed adjustment
- Date validated, confidence level (HIGH/MEDIUM/LOW)

If not validated, adjust the agent design and re-test. Maximum 2 iteration rounds before escalating to the user for guidance.
```

Keep the section concise (~40-50 lines). This is a behavioral guideline, not a detailed spec.
  </action>
  <verify>
    <automated>bash -c "grep -q 'Subagent Methodology' signe/agents/signe.md && grep -q 'agent-recipes' signe/agents/signe.md && echo PASS || echo FAIL"</automated>
  </verify>
  <done>signe.md contains a Subagent Methodology section with the 5-step cycle (Research, Design, Test, Validate, Bank) that references /signe-research, /signe-design preset:agent, sample task testing, validation criteria, and agent-recipes.md for banking. All existing sections remain unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Initialize agent-recipes.md and update MEMORY.md index</name>
  <files>~/.claude/agent-memory/signe/agent-recipes.md, ~/.claude/agent-memory/signe/MEMORY.md</files>
  <action>
**Create `~/.claude/agent-memory/signe/agent-recipes.md`:**

```markdown
# Validated Agent Patterns

Curated playbook of agent designs that have been tested and validated through Signe's methodology cycle (research -> design -> test -> validate -> bank). Each entry records what worked and what failed so future agent creation can learn from past results.

## Entry Format

Each validated pattern follows this structure:

### [Agent Name] - [Task Type]

**Validated:** [date]
**Model:** [model used during validation]
**Confidence:** HIGH | MEDIUM | LOW

**Prompt pattern:** [1-2 sentence summary of what makes this agent effective]

**What works:**
- [specific pattern that produced good results]

**What failed:**
- [pattern that was tried and produced poor results]

**Key metrics:**
- Sample tasks tested: [N]
- Success rate: [X/N]

---

## Patterns

No validated patterns yet. Entries will be added as Signe completes methodology cycles for new agents.

---

*Maintained by Signe's subagent methodology. Entries older than 90 days or for superseded model versions should be reviewed and pruned.*
```

**Update `~/.claude/agent-memory/signe/MEMORY.md`:**

Read the existing MEMORY.md. Replace the "No topics yet" line under `## Topics` with:

```markdown
- **agent-recipes.md** -- Validated agent design patterns. Records model, task type, prompt pattern, success/failure notes for each tested agent configuration.
```

Keep the rest of MEMORY.md unchanged.
  </action>
  <verify>
    <automated>bash -c "test -f ~/.claude/agent-memory/signe/agent-recipes.md && grep -q 'Validated Agent Patterns' ~/.claude/agent-memory/signe/agent-recipes.md && grep -q 'agent-recipes.md' ~/.claude/agent-memory/signe/MEMORY.md && echo PASS || echo FAIL"</automated>
  </verify>
  <done>agent-recipes.md exists with entry format template, instructions, and pruning guidance. MEMORY.md index references agent-recipes.md as a topic file. Both files are under the 200-line limit.</done>
</task>

</tasks>

<verification>
- signe.md has Subagent Methodology section with all 5 steps
- Methodology references /signe-research and /signe-design preset:agent
- Methodology defines concrete validation criteria (format, file refs, severity, success rate)
- agent-recipes.md exists with entry format and maintenance instructions
- MEMORY.md index lists agent-recipes.md
</verification>

<success_criteria>
- signe.md methodology section covers METH-01 through METH-05
- agent-recipes.md ready to receive banked patterns
- MEMORY.md topic index updated
</success_criteria>

<output>
After completion, create `.planning/phases/05-oversight-memory/05-02-SUMMARY.md`
</output>
