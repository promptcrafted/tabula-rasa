---
phase: 05-oversight-memory
plan: 04
type: execute
wave: 2
depends_on:
  - 05-03
files_modified:
  - ~/.claude/SIGNE-GUIDE.md
  - ~/.claude/agents/signe-test-agent.md
  - ~/.claude/hooks/signe-lifecycle.js
autonomous: true
requirements:
  - OVRS-06

must_haves:
  truths:
    - "SIGNE-GUIDE.md includes /signe-oversee in Quick Reference table"
    - "SIGNE-GUIDE.md includes oversight step in workflow chain diagram"
    - "SIGNE-GUIDE.md has a dedicated Oversight section with usage examples and scope options"
    - "signe-test-agent.md validates all current Signe files (18+ files, not 8)"
    - "signe-test-agent.md health report file count reflects actual file set"
    - "signe-lifecycle.js logs raw event payload in debug mode to diagnose field mismatches"
  artifacts:
    - path: "~/.claude/SIGNE-GUIDE.md"
      provides: "Complete user guide with all 5 modes documented"
      contains: "signe-oversee"
    - path: "~/.claude/agents/signe-test-agent.md"
      provides: "Health check covering all Phase 1-5 files"
      contains: "signe-overseer.md"
    - path: "~/.claude/hooks/signe-lifecycle.js"
      provides: "Lifecycle hook with debug logging for schema verification"
      contains: "JSON.stringify"
  key_links:
    - from: "~/.claude/SIGNE-GUIDE.md"
      to: "~/.claude/skills/signe-oversee/SKILL.md"
      via: "Guide documents /signe-oversee usage"
      pattern: "signe-oversee"
---

<objective>
Address findings from the 2026-03-08 oversight review (signe-review-2026-03-08-full.md).

Purpose: Fix the 3 actionable gaps identified by the overseer: incomplete user guide (COR-002, COR-003), stale health check (COR-005), and unverified hook schema (COR-001). These are quality issues that should be resolved before Phase 6.
Output: Updated SIGNE-GUIDE.md, signe-test-agent.md, and signe-lifecycle.js deployed to ~/.claude/.
</objective>

<execution_context>
@C:/Users/minta/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minta/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@signe-review-2026-03-08-full.md (source findings)
@.planning/phases/05-oversight-memory/05-03-PLAN.md (predecessor -- must complete first)

Review findings addressed:
- COR-002 (high): SIGNE-GUIDE.md missing /signe-oversee entirely
- COR-003 (medium): Workflow chain diagram incomplete (missing oversee step)
- COR-005 (medium): Health check validates 8 files but 18+ now exist
- COR-001 (medium): Hook field names (agent_type, agent_id) unverified against Claude Code schema

Review findings NOT addressed (out of scope for this plan):
- COV-001 (medium): No automated tests -- deferred; health check update partially mitigates
- SEC-001 (medium): Credentials file proximity -- environment concern, not a code change
- SEC-002 (low): bypassPermissions documentation -- accepted design decision
- STY-001 (low): maxTurns rationale -- cosmetic, defer to Phase 6
- STY-002 (low): Agent file sizes -- observation only, no action needed
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update SIGNE-GUIDE.md with oversight mode</name>
  <files>~/.claude/SIGNE-GUIDE.md</files>
  <action>
Fix COR-002 and COR-003: Add /signe-oversee to the user guide.

1. **Quick Reference table**: Add row after `/signe-design`:
   ```
   | `/signe-oversee [scope:lens] <target>` | Code review with quality gates | After building — verify quality and completeness |
   ```

2. **Workflow chain diagram**: Extend to include the oversight step:
   ```
   /signe-design  → reads research files, writes signe-design-*.md
            ↓
   /signe-oversee → reads plan files, writes signe-review-*.md
   ```

3. **Add Oversight section** after the Design section and before Best Practices. Include:
   - Description: Multi-lens code review comparing implementation against plans
   - Basic usage example: `/signe-oversee review the auth module`
   - Scoped usage example: `/signe-oversee scope:security review the API handlers`
   - Available lenses: security, correctness, performance, test-coverage, style
   - Scope options: `full` (default, all lenses), `scope:security`, `scope:correctness`, etc.
   - Output: `signe-review-[date]-[target].md` with findings, gap report, and quality gate verdict
   - Quality gate verdicts: PASS, WARN, FAIL with brief explanation of each

4. **Update Best Practices**: Add a bullet about using oversight after implementation:
   "**Review after building.** Run `/signe-oversee` after implementing a plan to verify quality and catch gaps before moving on."

5. **Update Troubleshooting**: Add entry:
   "**Review seems incomplete:** Use `scope:lens` to focus on a specific area, or ensure `.planning/` exists for plan gap analysis."
  </action>
  <verify>
    <automated>bash -c "grep -q 'signe-oversee' ~/.claude/SIGNE-GUIDE.md && grep -c 'signe-oversee' ~/.claude/SIGNE-GUIDE.md | xargs test 3 -le && echo PASS || echo FAIL"</automated>
  </verify>
  <done>SIGNE-GUIDE.md includes /signe-oversee in Quick Reference, workflow chain, dedicated section with examples, best practices, and troubleshooting.</done>
</task>

<task type="auto">
  <name>Task 2: Update health check to cover all current files</name>
  <files>~/.claude/agents/signe-test-agent.md</files>
  <action>
Fix COR-005: Update signe-test-agent.md to validate all Signe files from Phases 1-5.

1. **Replace the File Existence checklist** with the complete set:

   Core infrastructure:
   - `~/.claude/CLAUDE.md`
   - `~/.claude/SIGNE-GUIDE.md`
   - `~/.claude/settings.json` (verify contains signe patterns)

   Agents (6):
   - `~/.claude/agents/signe.md`
   - `~/.claude/agents/signe-test-agent.md`
   - `~/.claude/agents/signe-researcher.md`
   - `~/.claude/agents/signe-planner.md`
   - `~/.claude/agents/signe-designer.md`
   - `~/.claude/agents/signe-overseer.md`

   Skills (5):
   - `~/.claude/skills/signe-health/SKILL.md`
   - `~/.claude/skills/signe-research/SKILL.md`
   - `~/.claude/skills/signe-plan/SKILL.md`
   - `~/.claude/skills/signe-design/SKILL.md`
   - `~/.claude/skills/signe-oversee/SKILL.md`

   Rules (3):
   - `~/.claude/rules/signe-personality.md`
   - `~/.claude/rules/signe-delegation.md`
   - `~/.claude/rules/signe-safety.md`

   Hooks (1):
   - `~/.claude/hooks/signe-lifecycle.js`

   Memory (1):
   - `~/.claude/agent-memory/signe/agent-recipes.md`

2. **Update the report format** to reflect the new count:
   ```
   Files:     [X/20 passed]
   ```

3. **Add YAML validation** for all 6 agent files (not just signe.md):
   Read each agent file and confirm it has a `name:` field in YAML frontmatter matching its expected name.

4. **Add skill validation**: Read each of the 5 SKILL.md files and confirm each contains `agent:` in its frontmatter.
  </action>
  <verify>
    <automated>bash -c "grep -c '~/.claude/' ~/.claude/agents/signe-test-agent.md | xargs test 15 -le && echo PASS || echo FAIL"</automated>
  </verify>
  <done>signe-test-agent.md validates 20 files across agents, skills, rules, hooks, and memory. Report format updated. YAML validation covers all agents and skills.</done>
</task>

<task type="auto">
  <name>Task 3: Add debug mode to lifecycle hook for schema verification</name>
  <files>~/.claude/hooks/signe-lifecycle.js</files>
  <action>
Fix COR-001: Add diagnostic logging to verify event field names.

1. **Add debug output** before the field access: When `SIGNE_DEBUG=1` environment variable is set, log the raw event payload to stderr so the user can verify field names:
   ```javascript
   if (process.env.SIGNE_DEBUG === '1') {
     console.error(`[Signe DEBUG] Raw event: ${JSON.stringify(data)}`);
   }
   ```

2. **Add field presence warning**: If both `agent_type` and `agent_id` resolve to 'unknown', log a warning to stderr (always, not just in debug mode):
   ```javascript
   if (agentType === 'unknown' && agentId === 'unknown') {
     console.error(`[Signe WARN] No agent_type/agent_id in event. Fields present: ${Object.keys(data).join(', ')}`);
   }
   ```

This makes field mismatches observable rather than silently swallowed.
  </action>
  <verify>
    <automated>bash -c "grep -q 'SIGNE_DEBUG' ~/.claude/hooks/signe-lifecycle.js && grep -q 'Object.keys' ~/.claude/hooks/signe-lifecycle.js && echo PASS || echo FAIL"</automated>
  </verify>
  <done>signe-lifecycle.js has debug mode (SIGNE_DEBUG=1) for raw payload logging and always-on warning when field names don't match.</done>
</task>

</tasks>

<verification>
- SIGNE-GUIDE.md contains /signe-oversee in Quick Reference, workflow chain, and dedicated section
- signe-test-agent.md validates 20 files (up from 8) with YAML validation for all agents
- signe-lifecycle.js has debug mode and field mismatch warning
- All 3 files deployed to ~/.claude/
</verification>

<success_criteria>
- All 3 high/medium review findings (COR-001, COR-002/003, COR-005) have concrete fixes deployed
- Health check reflects the actual state of the Signe installation
- Hook schema mismatches are now observable instead of silent
</success_criteria>

<output>
After completion, create `.planning/phases/05-oversight-memory/05-04-SUMMARY.md`
</output>
