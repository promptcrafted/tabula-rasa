---
phase: 05-oversight-memory
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
  - 05-02
files_modified:
  - signe/CLAUDE.md
  - signe/rules/signe-delegation.md
autonomous: false

requirements:
  - OVRS-01
  - OVRS-02
  - OVRS-03
  - OVRS-04
  - OVRS-05
  - OVRS-06

must_haves:
  truths:
    - "CLAUDE.md mode table shows Oversight as Available"
    - "signe-delegation.md shows signe-overseer as Available"
    - "All signe files deployed to ~/.claude/ via cp"
    - "/signe-oversee spawns signe-overseer agent successfully"
    - "Overseer produces structured findings with file, line, severity, and recommended fix"
  artifacts:
    - path: "signe/CLAUDE.md"
      provides: "Updated mode table with Oversight Available"
      contains: "Available"
    - path: "signe/rules/signe-delegation.md"
      provides: "Updated routing table with signe-overseer Available"
      contains: "Available"
  key_links:
    - from: "signe/CLAUDE.md"
      to: "signe/skills/signe-oversee/SKILL.md"
      via: "Mode table references /signe-oversee skill"
      pattern: "signe-oversee"
    - from: "signe/rules/signe-delegation.md"
      to: "signe/agents/signe-overseer.md"
      via: "Routing table maps skill to agent"
      pattern: "signe-overseer"
---

<objective>
Update Signe infrastructure files to mark Oversight as Available, deploy all files to ~/.claude/, and validate /signe-oversee end-to-end.

Purpose: Makes the overseer live in the global installation so users can invoke /signe-oversee from any project. Validates the complete flow works by having a human verify the review output.
Output: Updated infrastructure files, deployed global installation, human-verified oversight capability.
</objective>

<execution_context>
@C:/Users/minta/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minta/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-oversight-memory/05-01-SUMMARY.md
@.planning/phases/05-oversight-memory/05-02-SUMMARY.md
@.planning/phases/04-design-modes/04-02-SUMMARY.md

Reference for integration pattern:
@signe/CLAUDE.md (mode table -- update Oversight row)
@signe/rules/signe-delegation.md (routing table -- update signe-overseer row)
@signe/agents/signe.md (Coming Soon section -- move /signe-oversee to Now)

<interfaces>
<!-- Files to update follow the pattern established in 04-02-SUMMARY -->

From signe/CLAUDE.md (line to change):
```
| Oversight | `/signe-oversee` | Code review, quality gates, progress tracking | (Phase 5) |
```
Change to:
```
| Oversight | `/signe-oversee` | Code review, quality gates, progress tracking | Available |
```

From signe/rules/signe-delegation.md (line to change):
```
| `/signe-oversee` | `signe-overseer` | Code review, quality gates | (Phase 5) |
```
Change to:
```
| `/signe-oversee` | `signe-overseer` | Code review, quality gates | Available |
```

Phase note to update (currently says Phase 4, change to Phase 5):
```
**Phase 4 note:** `signe-test-agent`, `signe-researcher`, `signe-planner`, and `signe-designer` are available...
```
Change to:
```
**Phase 5 note:** `signe-test-agent`, `signe-researcher`, `signe-planner`, `signe-designer`, and `signe-overseer` are available. All other mode agents are future phase deliverables. Do not attempt to spawn agents that do not exist.
```

From signe/agents/signe.md (move /signe-oversee from Coming Soon to Now):
Move the `/signe-oversee` line from "### Coming Soon" to "### Now" section.
If Coming Soon section is empty after this, replace contents with a note about Phase 6 capabilities.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update infrastructure files and deploy to ~/.claude/</name>
  <files>signe/CLAUDE.md, signe/rules/signe-delegation.md, signe/agents/signe.md</files>
  <action>
Follow the exact integration pattern from Phase 4 (04-02-SUMMARY.md):

1. **Update signe/CLAUDE.md**: Change Oversight row status from `(Phase 5)` to `Available` in the mode table.

2. **Update signe/agents/signe.md**: Move `/signe-oversee` from the "Coming Soon" section to the "Now" section. Add it as:
   ```
   - `/signe-oversee` -- Code review, quality gates, progress tracking, and plan gap analysis. Multi-lens review (security, correctness, performance, test coverage, style) with quality gate verdicts.
   ```
   Update "Coming Soon" to only reference Phase 6 capabilities, or remove the section if nothing remains.

3. **Update signe/rules/signe-delegation.md**:
   - Change signe-overseer row status from `(Phase 5)` to `Available`
   - Update the phase note from "Phase 4 note" to "Phase 5 note" and add `signe-overseer` to the list of available agents

4. **Deploy to ~/.claude/** using direct cp pattern (consistent with Phases 2, 3, 4):
   ```bash
   cp -r signe/agents/* ~/.claude/agents/
   cp -r signe/skills/* ~/.claude/skills/
   cp -r signe/rules/* ~/.claude/rules/
   cp signe/CLAUDE.md ~/.claude/CLAUDE.md
   ```

Do NOT modify settings.json -- the existing `Agent(signe-*)` and `Skill(signe-*)` patterns already cover the new agent and skill.
  </action>
  <verify>
    <automated>bash -c "grep -q 'Available' signe/CLAUDE.md && grep 'signe-oversee' signe/rules/signe-delegation.md | grep -q 'Available' && test -f ~/.claude/agents/signe-overseer.md && test -f ~/.claude/skills/signe-oversee/SKILL.md && echo PASS || echo FAIL"</automated>
  </verify>
  <done>CLAUDE.md shows Oversight as Available, delegation rules show signe-overseer as Available with Phase 5 note, signe.md lists /signe-oversee under Now. All files deployed to ~/.claude/ global installation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end validation of /signe-oversee</name>
  <files>N/A</files>
  <action>
Human validates that /signe-oversee works end-to-end. This is a human-verify checkpoint -- the executor pauses and asks the user to test.

Test the oversight agent from any project directory:

1. Open a new Claude Code session (or use `/clear` in current session)
2. Invoke: `/signe-oversee review the signe project in this directory`
3. Verify the overseer:
   - Spawns as signe-overseer agent (check agent name in output)
   - Produces findings organized by lens (security, correctness, performance, test-coverage, style sections)
   - Each finding includes file path, line number, severity level, and recommended fix
   - Produces a plan gap analysis table (if .planning/ exists)
   - Produces a progress report
   - Produces a quality gate verdict (PASS/WARN/FAIL)
   - Writes a review report file (signe-review-*.md)
4. Optionally test a scoped review: `/signe-oversee scope:security review the signe agents`
5. Verify scoped review only produces security lens findings
  </action>
  <verify>Human confirms structured review output with all expected sections</verify>
  <done>Human approved that /signe-oversee produces multi-lens review findings with file/line references, plan gap analysis, progress report, and quality gate verdict</done>
</task>

</tasks>

<verification>
- CLAUDE.md mode table shows Oversight as Available (not Phase 5)
- signe-delegation.md routing table shows signe-overseer as Available
- signe.md lists /signe-oversee under Now, not Coming Soon
- All files deployed to ~/.claude/ global installation
- Human verified: /signe-oversee produces structured multi-lens review with quality gate verdict
</verification>

<success_criteria>
- Infrastructure files updated following exact Phase 2/3/4 integration pattern
- Deployed to ~/.claude/ via direct cp
- Human confirmed /signe-oversee works end-to-end with structured output
</success_criteria>

<output>
After completion, create `.planning/phases/05-oversight-memory/05-03-SUMMARY.md`
</output>
