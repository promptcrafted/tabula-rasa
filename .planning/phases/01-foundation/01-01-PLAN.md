---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - signe/CLAUDE.md
  - signe/rules/signe-personality.md
  - signe/rules/signe-delegation.md
  - signe/rules/signe-safety.md
  - signe/hooks/signe-lifecycle.js
  - signe/agent-memory/signe/MEMORY.md
autonomous: true
requirements: [INFRA-03, INFRA-04, INFRA-06, INFRA-07]

must_haves:
  truths:
    - "CLAUDE.md is under 100 lines with identity, delegation rule, and flat orchestration constraint in the first 10 lines"
    - "Three rules files exist covering personality, delegation, and safety as separate concerns"
    - "All Signe files use the signe- prefix naming convention"
    - "Hook script handles SubagentStart and SubagentStop events with stdout output and 3-second stdin timeout guard"
    - "Memory directory exists with an initial MEMORY.md index file"
  artifacts:
    - path: "signe/CLAUDE.md"
      provides: "Global identity and delegation instructions"
      max_lines: 100
    - path: "signe/rules/signe-personality.md"
      provides: "Chief of staff behavioral guidelines"
      min_lines: 30
    - path: "signe/rules/signe-delegation.md"
      provides: "Subagent delegation rules and anti-patterns"
      min_lines: 30
    - path: "signe/rules/signe-safety.md"
      provides: "Safety constraints (no nesting, no autonomous execution)"
      min_lines: 15
    - path: "signe/hooks/signe-lifecycle.js"
      provides: "SubagentStart/Stop lifecycle logging"
      min_lines: 20
    - path: "signe/agent-memory/signe/MEMORY.md"
      provides: "Initial empty memory index"
      min_lines: 3
  key_links:
    - from: "signe/CLAUDE.md"
      to: "signe/rules/signe-*.md"
      via: "reference to rules files in delegation section"
      pattern: "rules/signe-"
    - from: "signe/hooks/signe-lifecycle.js"
      to: "stdin JSON input"
      via: "parses hook_event_name and agent_type from Claude Code"
      pattern: "data\\.hook_event_name"
---

<objective>
Create the Signe identity layer and supporting infrastructure: CLAUDE.md (under 100 lines), three rules files (personality, delegation, safety), the Node.js lifecycle hook script, and the initial memory directory.

Purpose: Establishes Signe's identity, behavioral rules, lifecycle hooks, and memory foundation. Every subsequent phase builds on this skeleton.
Output: 6 files in `signe/` directory ready for file-drop installation to `~/.claude/`.
</objective>

<execution_context>
@C:/Users/minta/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minta/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CLAUDE.md and rules files</name>
  <files>signe/CLAUDE.md, signe/rules/signe-personality.md, signe/rules/signe-delegation.md, signe/rules/signe-safety.md</files>
  <action>
Create the `signe/` directory tree and four markdown files.

**signe/CLAUDE.md** (MUST be under 100 lines total):

First 10 lines MUST contain:
1. `# Signe -- Chief of Staff` heading
2. One-line identity: "You have access to Signe, a chief of staff agent who manages research, planning, design, and oversight workflows. She delegates to specialized subagents and builds a persistent playbook of what works."
3. Delegation rule: "When invoking Signe workflows, use the designated skills: `/signe-research`, `/signe-plan`, `/signe-design`, `/signe-oversee`. For multi-mode workflows, invoke `/signe` directly."
4. Critical constraint: "All subagent spawning happens from the main thread only. Subagents CANNOT spawn other subagents."
5. Reference: "See `.claude/rules/signe-*.md` for detailed behavioral rules."

Remaining lines (up to ~90 more) should include:
- Available modes section listing research, planning, design (4 presets), oversight with brief descriptions
- Note that modes not yet implemented say "(Phase N)" next to them
- For Phase 1: only `/signe-health` is available. All mode skills are marked as future phases.
- Tool preferences section noting Signe uses all available MCP tools in the environment
- Memory note: Signe accumulates cross-project knowledge in agent-memory

Do NOT include personality details (those go in rules/signe-personality.md).
Do NOT duplicate delegation details (those go in rules/signe-delegation.md).

All `signe-` prefix naming convention must be consistently applied per INFRA-03.

**signe/rules/signe-personality.md** (~40-60 lines):
- Chief of staff behavioral guidelines
- Communication style: direct, proactive, surfaces what matters
- Proactive risk identification -- flag blockers before asked
- Milestone summaries -- summarize status at natural milestones
- Recommends next actions based on current state
- Maker-checker mindset -- design produces, review critiques, iterate
- Does NOT include delegation rules (that is signe-delegation.md)

**signe/rules/signe-delegation.md** (~40-60 lines):
- When and how to spawn subagents
- Decision tree: which mode to use for which task type
- Anti-patterns: no nested spawning, no over-delegation (>5 concurrent agents), no generic role agents
- Flat orchestrator enforcement -- only the main signe.md thread spawns agents
- Skill-to-agent mapping table (which skill routes to which agent)
- For Phase 1: note that only signe-test-agent is available; mode agents are future phases

**signe/rules/signe-safety.md** (~20-30 lines):
- What Signe must NEVER do:
  - No nested subagent spawning (subagents cannot spawn subagents)
  - No autonomous execution beyond user-approved checkpoints
  - No modification of GSD files or workflows (Phase 6 integration)
  - No over-parallelization (max 5 concurrent subagents)
  - No generic role agents (always use feature-specific agents with concrete context)
- What Signe must ALWAYS do:
  - Use `signe-` prefix for all file names
  - Use `memory: user` scope for cross-project knowledge persistence
  - Output to stdout only (no log files)
  </action>
  <verify>
    <automated>wc -l signe/CLAUDE.md &amp;&amp; test $(wc -l &lt; signe/CLAUDE.md) -lt 101 &amp;&amp; echo "CLAUDE.md line count OK" || echo "CLAUDE.md EXCEEDS 100 lines"; head -10 signe/CLAUDE.md; ls -la signe/rules/signe-*.md</automated>
  </verify>
  <done>CLAUDE.md exists with under 100 lines, first 10 lines contain identity + delegation rule + flat orchestration constraint. Three rules files exist at signe/rules/ with signe- prefix. No personality or delegation detail duplication in CLAUDE.md.</done>
</task>

<task type="auto">
  <name>Task 2: Create lifecycle hook and memory directory</name>
  <files>signe/hooks/signe-lifecycle.js, signe/agent-memory/signe/MEMORY.md</files>
  <action>
**signe/hooks/signe-lifecycle.js**:
Create a Node.js hook script that handles both SubagentStart and SubagentStop lifecycle events. Follow the proven GSD hook pattern from `~/.claude/hooks/gsd-check-update.js`.

Requirements:
- Shebang line: `#!/usr/bin/env node`
- 3-second stdin timeout guard: `const stdinTimeout = setTimeout(() => process.exit(0), 3000);` -- this prevents hanging on Windows
- Read JSON from stdin via `process.stdin` with `data` and `end` events
- Parse `hook_event_name`, `agent_type`, and `agent_id` from the JSON input
- Only process agents with `signe-` prefix (double-check even though `matcher: "signe-.*"` in settings handles it)
- On SubagentStart: `console.log("[Signe HH:MM:SS] Started: {agent_type} ({agent_id})")`
- On SubagentStop: `console.log("[Signe HH:MM:SS] Stopped: {agent_type} ({agent_id})")`
- Timestamp format: `new Date().toISOString().slice(11, 19)` for HH:MM:SS
- Silent catch for JSON parse errors -- never break the session
- `process.exit(0)` at end of processing

Use the exact patterns from the research RESEARCH.md "Pattern 3: Hook Script with stdin Timeout Guard" section. This is a proven Windows-safe pattern.

**signe/agent-memory/signe/MEMORY.md**:
Create an initial memory index file with:
- `# Signe Memory` heading
- Brief description: "Cross-project knowledge accumulated by Signe agents. Auto-loaded on agent start (first 200 lines)."
- `## Topics` section with a note: "No topics yet. Topics will be added as agents validate patterns."
- `## Index` section explaining that topic files live alongside this index when content exceeds 200 lines
- Keep under 20 lines total
  </action>
  <verify>
    <automated>node -c signe/hooks/signe-lifecycle.js &amp;&amp; echo "Hook syntax OK"; cat signe/agent-memory/signe/MEMORY.md; ls -la signe/hooks/ signe/agent-memory/signe/</automated>
  </verify>
  <done>Hook script passes Node.js syntax check, contains stdin timeout guard and signe- prefix filter. MEMORY.md exists with heading and empty topics section.</done>
</task>

</tasks>

<verification>
- CLAUDE.md is under 100 lines (verified by `wc -l`)
- First 10 lines of CLAUDE.md contain identity, delegation rule, and flat orchestration constraint
- All file names use `signe-` prefix (INFRA-03)
- Three separate rules files exist covering personality, delegation, safety
- Hook script passes `node -c` syntax validation
- Hook script contains 3-second stdin timeout guard pattern
- MEMORY.md exists at correct path
</verification>

<success_criteria>
- signe/CLAUDE.md exists, under 100 lines, first 10 lines have identity + delegation + constraint
- signe/rules/ contains exactly 3 files: signe-personality.md, signe-delegation.md, signe-safety.md
- signe/hooks/signe-lifecycle.js is valid Node.js with stdin timeout guard
- signe/agent-memory/signe/MEMORY.md exists with initial structure
- All files use signe- prefix naming convention
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
