---
phase: 09-packaging-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - install.sh
  - agents/signe.md
  - agents/signe-designer.md
  - agents/signe-overseer.md
  - agents/signe-planner.md
  - agents/signe-researcher.md
  - agents/signe-setup-agent.md
  - agents/signe-test-agent.md
  - rules/signe-delegation.md
  - rules/signe-personality.md
  - rules/signe-safety.md
  - skills/signe/SKILL.md
  - skills/signe-design/SKILL.md
  - skills/signe-health/SKILL.md
  - skills/signe-oversee/SKILL.md
  - skills/signe-plan/SKILL.md
  - skills/signe-research/SKILL.md
  - skills/signe-setup/SKILL.md
  - skills/signe-reset-persona/SKILL.md
  - templates/CLAUDE.md
  - hooks/signe-lifecycle.js
  - settings-merge.json
autonomous: true
requirements: [PKG-01]

must_haves:
  truths:
    - "User clones repo and sees agent files at top level (agents/, skills/, rules/, templates/)"
    - "User runs install.sh and all files are copied to ~/.claude/ with correct structure"
    - "Install script detects existing files and backs them up before overwriting"
    - "Install script handles CLAUDE.md specially -- never overwrites, offers to append"
    - "Install script is idempotent -- safe to re-run for updates"
  artifacts:
    - path: "install.sh"
      provides: "Bash install script with conflict detection and backup"
      min_lines: 80
    - path: "agents/signe.md"
      provides: "Main orchestrator agent (synced from ~/.claude/)"
    - path: "templates/CLAUDE.md"
      provides: "Template CLAUDE.md with placeholder sections"
    - path: "skills/signe-setup/SKILL.md"
      provides: "Setup skill (new in Phase 8)"
    - path: "skills/signe-reset-persona/SKILL.md"
      provides: "Reset persona skill (new in Phase 8)"
  key_links:
    - from: "install.sh"
      to: "~/.claude/"
      via: "cp commands"
      pattern: "cp.*\\.claude/"
---

<objective>
Restructure the repo for public release and create the install script.

Purpose: The existing `signe/` subdirectory has stale v1.0 files. The repo needs to be restructured with agent files at the top level (per CONTEXT.md decisions), synced with the latest scrubbed files from `~/.claude/`, and an `install.sh` script created for one-command installation.

Output: Repo with `agents/`, `skills/`, `rules/`, `templates/`, `hooks/` at root level, plus `install.sh` with conflict detection and backup.
</objective>

<execution_context>
@C:/Users/minta/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minta/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-packaging-documentation/09-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure repo and sync agent files from ~/.claude/</name>
  <files>agents/signe.md, agents/signe-designer.md, agents/signe-overseer.md, agents/signe-planner.md, agents/signe-researcher.md, agents/signe-setup-agent.md, agents/signe-test-agent.md, rules/signe-delegation.md, rules/signe-personality.md, rules/signe-safety.md, skills/signe/SKILL.md, skills/signe-design/SKILL.md, skills/signe-health/SKILL.md, skills/signe-oversee/SKILL.md, skills/signe-plan/SKILL.md, skills/signe-research/SKILL.md, skills/signe-setup/SKILL.md, skills/signe-reset-persona/SKILL.md, templates/CLAUDE.md, hooks/signe-lifecycle.js, settings-merge.json</files>
  <action>
    1. Remove the old `signe/` subdirectory entirely (it has stale v1.0 files)
    2. Remove the stale `signe-review-2026-03-08-full.md` file from repo root if present
    3. Create the new top-level repo structure:
       ```
       agents/          <- copy from ~/.claude/agents/signe*.md (7 files)
       skills/          <- copy from ~/.claude/skills/signe*/ (8 directories, each with SKILL.md)
       rules/           <- copy from ~/.claude/rules/signe-*.md (3 files)
       templates/       <- copy ~/.claude/CLAUDE.md as templates/CLAUDE.md
       hooks/           <- copy from signe/hooks/ (signe-lifecycle.js)
       settings-merge.json <- copy from signe/settings-merge.json
       ```
    4. The source of truth is `~/.claude/` for agents, skills, rules, and CLAUDE.md (Phase 7-8 updated these files in place)
    5. For hooks and settings-merge.json, copy from the old `signe/` directory before deleting it (these were not modified in Phase 7-8)
    6. Verify file counts: 7 agents, 8 skill directories, 3 rules, 1 template, 1 hook, 1 settings file = 21 files total
    7. Do NOT include `agent-memory/` in the repo -- the install script will create the empty directory structure
    8. Do NOT include `.planning/` or any GSD files in the packaged structure
  </action>
  <verify>
    <automated>ls agents/signe*.md | wc -l && ls skills/signe*/SKILL.md | wc -l && ls rules/signe-*.md | wc -l && test -f templates/CLAUDE.md && test -f hooks/signe-lifecycle.js && test -f settings-merge.json && echo "All files present" || echo "MISSING FILES"</automated>
  </verify>
  <done>Repo root has agents/ (7 files), skills/ (8 dirs with SKILL.md each), rules/ (3 files), templates/CLAUDE.md, hooks/signe-lifecycle.js, settings-merge.json. Old signe/ directory removed.</done>
</task>

<task type="auto">
  <name>Task 2: Create install.sh with conflict detection and backup</name>
  <files>install.sh</files>
  <action>
    Create `install.sh` at repo root. Pure bash, no dependencies beyond coreutils and git.

    **Pre-flight checks:**
    - Verify `claude` CLI is installed (`claude --version` or `command -v claude`). If not found, print helpful message and exit.
    - Verify `~/.claude/` directory exists. If not, suggest running `claude` once first to create it.
    - Detect script's own directory (`SCRIPT_DIR`) to find source files.

    **Conflict detection and backup:**
    - Before copying, check if any target files already exist in `~/.claude/`
    - If conflicts found: create backup at `~/.claude/backups/tabula-rasa-$(date +%Y%m%d-%H%M%S)/`
    - Copy existing conflicting files to backup directory preserving structure
    - Print summary of backed-up files

    **Copy operations:**
    - `agents/signe*.md` -> `~/.claude/agents/`
    - `skills/signe*/` -> `~/.claude/skills/` (preserve directory structure)
    - `rules/signe-*.md` -> `~/.claude/rules/`
    - `hooks/signe-lifecycle.js` -> `~/.claude/hooks/`
    - `settings-merge.json` -> print note that user should manually merge with existing `~/.claude/settings.json` (do NOT auto-overwrite settings.json)

    **CLAUDE.md special handling:**
    - If `~/.claude/CLAUDE.md` does NOT exist: copy `templates/CLAUDE.md` to `~/.claude/CLAUDE.md`
    - If `~/.claude/CLAUDE.md` DOES exist: print message that template is at `templates/CLAUDE.md` and suggest user manually merge the agent section. Do NOT overwrite.

    **agent-memory directory:**
    - Create `~/.claude/agent-memory/signe/` if it doesn't exist
    - Do NOT overwrite existing MEMORY.md (persona data lives there)

    **Post-install:**
    - Print success message with file counts
    - Suggest running `/signe-health` to verify
    - Suggest running `/setup` for first-time users
    - Make script executable (include `#!/usr/bin/env bash` shebang)

    **Windows note:** Add comment at top that Windows users need Git Bash.
  </action>
  <verify>
    <automated>bash -n install.sh && echo "Syntax OK" || echo "Syntax error"</automated>
  </verify>
  <done>install.sh exists at repo root, passes bash syntax check, contains pre-flight checks, conflict detection with backup to ~/.claude/backups/, file copy operations for all agent components, special CLAUDE.md handling (never overwrites), agent-memory directory creation, and post-install guidance.</done>
</task>

</tasks>

<verification>
- All agent files from ~/.claude/ are present in repo at correct paths
- install.sh passes `bash -n` syntax validation
- Old signe/ directory is removed
- File counts match: 7 agents, 8 skill dirs, 3 rules, 1 template, 1 hook, 1 settings file
</verification>

<success_criteria>
Repo is structured for public consumption with all Phase 7-8 updates included, and install.sh provides one-command installation with conflict detection.
</success_criteria>

<output>
After completion, create `.planning/phases/09-packaging-documentation/09-01-SUMMARY.md`
</output>
