---
phase: 02-research-mode
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - signe/CLAUDE.md
  - signe/agents/signe.md
  - signe/rules/signe-delegation.md
autonomous: false

requirements: [RSRCH-01, RSRCH-02, RSRCH-06]

must_haves:
  truths:
    - "CLAUDE.md shows Research mode status as Available (not Phase 2)"
    - "signe.md orchestrator lists /signe-research under Now section (not Coming Soon)"
    - "signe-delegation.md routing table shows signe-researcher as Available"
    - "/signe-research invocation from any project directory spawns signe-researcher and produces structured research output"
  artifacts:
    - path: "signe/CLAUDE.md"
      provides: "Updated mode availability table"
      contains: "Available"
    - path: "signe/agents/signe.md"
      provides: "Orchestrator awareness of research mode"
      contains: "/signe-research"
    - path: "signe/rules/signe-delegation.md"
      provides: "Updated routing table"
      contains: "Available"
  key_links:
    - from: "signe/CLAUDE.md"
      to: "signe/skills/signe-research/SKILL.md"
      via: "Status table references /signe-research as Available"
      pattern: "Research.*Available"
    - from: "signe/agents/signe.md"
      to: "signe/agents/signe-researcher.md"
      via: "Now section lists /signe-research as active capability"
      pattern: "signe-research"
    - from: "signe/rules/signe-delegation.md"
      to: "signe/agents/signe-researcher.md"
      via: "Routing table maps /signe-research to signe-researcher"
      pattern: "signe-researcher.*Available"
---

<objective>
Update existing Signe files to mark research mode as Available, deploy all files to ~/.claude/, and validate end-to-end that /signe-research works from a project directory.

Purpose: The agent and skill created in Plan 01 need to be wired into the existing Signe infrastructure (CLAUDE.md, orchestrator, delegation rules) and deployed to the global installation. Without this, the skill won't be discoverable or routable.

Output: Updated signe/CLAUDE.md, signe/agents/signe.md, signe/rules/signe-delegation.md, all deployed to ~/.claude/, validated with a live research invocation.
</objective>

<execution_context>
@C:/Users/minta/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minta/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-research-mode/02-01-SUMMARY.md

<interfaces>
<!-- Files to update -- current content that must be modified. -->

From signe/CLAUDE.md line 15 (mode table -- change Research row):
```markdown
| Research | `/signe-research` | Multi-source investigation with confidence scoring | (Phase 2) |
```
Change to:
```markdown
| Research | `/signe-research` | Multi-source investigation with confidence scoring | Available |
```

From signe/agents/signe.md lines 24-27 (Coming Soon section -- move research to Now):
```markdown
### Now
- `/signe-health` -- Installation diagnostic...

### Coming Soon
- `/signe-research` (Phase 2) -- Deep-dive research...
```

From signe/rules/signe-delegation.md line 24 (routing table -- change status):
```markdown
| `/signe-research` | `signe-researcher` | Multi-source investigation | (Phase 2) |
```
Change to:
```markdown
| `/signe-research` | `signe-researcher` | Multi-source investigation | Available |
```

Also update the "Phase 1 note" at line 30 to reflect Phase 2 availability.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update existing Signe files and deploy to ~/.claude/</name>
  <files>signe/CLAUDE.md, signe/agents/signe.md, signe/rules/signe-delegation.md</files>
  <action>
**Update 3 files in repo (signe/ directory):**

1. **signe/CLAUDE.md** -- In the Available Modes table, change the Research row status from `(Phase 2)` to `Available`.

2. **signe/agents/signe.md** -- In the Available Capabilities section:
   - Move `/signe-research` from "Coming Soon" to "Now" section.
   - Update its description to remove "(Phase 2)" prefix: "Deep-dive research using web search, paper reading, library docs, and structured analysis. Multi-source with confidence scoring and domain-specific presets (ecosystem, feasibility, comparison, state-of-the-art)."
   - Keep all other Coming Soon items unchanged.

3. **signe/rules/signe-delegation.md** -- In the Skill-to-Agent Routing table:
   - Change the `/signe-research` row status from `(Phase 2)` to `Available`.
   - Update the "Phase 1 note" to "Phase 2 note" mentioning that signe-test-agent and signe-researcher are available. Other mode agents remain future deliverables.

**Deploy to ~/.claude/:**

After updating the repo files, copy ALL signe files to the global installation:

```bash
# Copy new files
cp signe/agents/signe-researcher.md ~/.claude/agents/
mkdir -p ~/.claude/skills/signe-research
cp signe/skills/signe-research/SKILL.md ~/.claude/skills/signe-research/

# Copy updated files
cp signe/CLAUDE.md ~/.claude/
cp signe/agents/signe.md ~/.claude/agents/
cp signe/rules/signe-delegation.md ~/.claude/rules/
```

Verify deployment by checking all files exist at their ~/.claude/ locations.
  </action>
  <verify>
    <automated>bash -c "grep -q 'Available' 'C:/Users/minta/Projects/scale-research/signe/CLAUDE.md' && test -f ~/.claude/agents/signe-researcher.md && test -f ~/.claude/skills/signe-research/SKILL.md && grep -q 'Available' ~/.claude/rules/signe-delegation.md && echo PASS || echo FAIL"</automated>
  </verify>
  <done>Three repo files updated with research mode marked Available. All Signe files deployed to ~/.claude/ including new agent and skill. Deployment verified by checking file existence at global installation paths.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end validation of /signe-research</name>
  <files>none (validation only)</files>
  <action>
Human verifies that the complete research mode works end-to-end.

**What was built:** Complete research mode -- signe-researcher agent with multi-source research methodology, /signe-research skill with preset support, updated CLAUDE.md/signe.md/delegation rules, all deployed to ~/.claude/.

**How to verify:**
1. Open a NEW Claude Code session (fresh context, any project directory)
2. Run: `/signe-research Claude Code subagent best practices 2026`
3. Verify the output contains:
   - Structured Markdown with "Research:" title
   - Executive Summary section
   - Findings with inline citations [Source](URL)
   - Confidence levels (HIGH/MEDIUM/LOW) on findings
   - Source Hierarchy table
   - Full Source List with URLs and dates
4. Verify no permission prompts appeared during research
5. Verify the researcher used multiple source types (not just WebSearch)
6. (Optional) Test a preset: `/signe-research preset:comparison React vs Vue for dashboards`

**Resume signal:** Type "approved" if research output meets quality bar, or describe issues to fix.
  </action>
  <verify>Human confirms research output quality and structure</verify>
  <done>/signe-research produces structured findings with inline citations, confidence levels, source hierarchy, and preset routing from any project directory without permission prompts.</done>
</task>

</tasks>

<verification>
- All 3 existing files updated with "Available" status for research mode
- All files deployed to ~/.claude/ (new + updated)
- /signe-research produces structured research output with citations and confidence levels
- No permission prompts during research execution
- Multiple source types used (WebSearch, WebFetch, and ideally MCP tools)
</verification>

<success_criteria>
- Research mode fully operational: user types `/signe-research [topic]` and gets structured findings
- Output matches the template defined in signe-researcher.md system prompt
- Confidence levels are present and appropriately assigned based on source quality
- Presets route correctly (preset:comparison produces comparison table, etc.)
- Human verification confirms quality bar met
</success_criteria>

<output>
After completion, create `.planning/phases/02-research-mode/02-02-SUMMARY.md`
</output>
